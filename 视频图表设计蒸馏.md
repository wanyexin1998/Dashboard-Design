# 图表实验室 · Chart Studies — 视觉与交互基准文档

> **基准文件**：`chart-studies.html`（REFERENCE EDITION / 01，2026-09-22；2026-09-23 增加浅色图表主题，深色与基准逐像素一致；同日新增「指标与概览」分组：8 种指标卡 + 6 种指标面板；同日再增「筛选与查询 / 切换与导航 / 表格与明细」三组 22 个组件，并加入页面级截至日与地址状态）
> **来源**：抖音 @叨叨 AI《10个图表交互控件提升App和小程序的质感》的视觉 / 交互蒸馏 → 首版参考页 → 经修改扩展为本基准版（15 个图表控件）→ 按《指标卡与指标面板方案》扩充指标组件（14 个）→ 按筛选器 / Tab / 表格三份方案扩充 22 个组件。
> **文档用途**：本文件描述基准版**实际实现**的页面架构、设计令牌、交互系统与每个控件的规格，是后续开发与正式设计的对齐依据。视频原始蒸馏内容保留在附录 A，作为出处与差异对照。
> **更新规则**：改 HTML 必改本文对应小节；新增控件按第 7 节的约定登记；与视频的偏离写入第 6 节。

---

## 0. 一页总览

| 项 | 基准值 |
|---|---|
| 控件数 | 51 = 图表 15（视频原有 10 + 新增 5：折线图、双柱＋折线组合图、瀑布图、四象限图、树状图）+ 指标组件 14（指标卡 8 · 指标面板 6）+ 筛选 / 页签 / 表格 22（筛选 11 · 页签 6 · 表格 5） |
| 分组 | 进度与目标 03 · 时间与趋势 07 · 组成与分类 03 · 结构与策略 02 · 指标与概览 14 · 筛选与查询 11 · 切换与导航 06 · 表格与明细 05 |
| 技术 | 纯 HTML / CSS / SVG / JS，无网络依赖；单文件约 490 KB |
| 页面 | 顶栏 → 引言 → 筛选工具条 → 组件网格（图表 · 指标卡 · 指标面板 · 筛选 · 页签 · 表格六段，后五段各以一条分隔标题开头）→ 设计规范 → 交互说明 → 指标规则 → 查询规则 → 页脚 |
| 全局能力 | 分类筛选、**图表底色深 / 浅切换**（记忆选择、切换不丢状态）、减少动态效果开关（同步系统偏好）、重播全部 / 单卡重播、统一**读数浮层**（悬浮 / 点选 / 键盘 / 窄屏停靠）、**页面级截至日**（F11 设定；22 个筛选 / 页签 / 表格组件与 K04、K05 按同一天重算，15 个图表与其余指标组件固定在 9/22；顶部胶囊显示）、**地址状态**（筛选与页签写进 `#!` 哈希，可分享、可后退） |
| 色彩体系 | 一套语义 token（`--c-*` 结构与文字、`--s-*` 系列、`--seq-*` 热力、`--st-*` 状态与胶囊底），深色为默认，浅色逐项选定；见第 2.2 节。筛选 / 页签 / 表格没有新增 token |
| 无障碍 | 每个控件均可键盘操作；role / aria-pressed / aria-live 完整；焦点环可见；筛选 / 页签 / 表格按 W3C APG 模式实现（select-only combobox、多选 listbox、日期网格、tabs 自动 / 手动激活、可排序表格与跨列表头） |
| 响应式 | 版心 1224px（≥1500：1320px）；≥901px 两列；≤960 隐藏品牌英文副标、版次与引言统计，工具条换行，卡片左右内边距改为 18，卡头英文名隐藏；701–900 单列居中（≤630px）；≤700 单列 + 指标停靠区，卡头英文名恢复，筛选胶囊隐藏计数；≤380 进一步压缩（卡头英文名再次隐藏）；纵向页签 ≤700 变横向，弹层上下翻转，顶栏导航 ≤700 单行滚动 |
| QA 状态 | Chromium 无头验证 深 / 浅 × 1320 / 800 / 390 三档 + 各控件交互：无横向溢出、控制台 0 报错；指标组件另跑 50 项交互断言（两套主题各 50/50 通过）；筛选 / 页签 / 表格另跑 98 项交互断言（两套主题各 98/98 通过）；第一阶段收尾修复回归 26 项；读数浮层移开 / 触屏 / 扫动检查；深色与基准逐卡像素比对平均差 ≤0.05/255（见第 8 节）。`npm test` 一键跑全部（见 7 · 11） |

---

## 1. 页面架构

### 1.1 区块

| 区块 | 内容 | 关键样式 |
|---|---|---|
| `<head>` | `<title>`「图表实验室 · Chart Studies」；description「51 种可交互组件参考模板：15 种图表、14 种指标组件、22 种筛选 / 页签 / 表格。」；`color-scheme: light dark`；`</style>` 后一段内联脚本：渲染前写主题（见 3.7）+ 记录输入方式 `data-input`（见第 5 节） | |
| `.topbar` | 品牌「图表实验室 / CHART STUDIES」、锚点导航（组件总览 `#collection` · 设计规范 `#tokens` · 交互说明 `#behaviors` · 指标规则 `#metrics` · 查询规则 `#query`）、版次「REFERENCE EDITION / 01」 | sticky，高 76px（≤700：62px），纸色 96% + 12px 模糊，`scroll-padding-top: 92px`；≤960 隐藏品牌英文副标与版次；≤700px 导航单行横向滚动、右缘渐隐 |
| `.intro` | Kicker「VISUAL SYSTEMS / INTERACTIVE COMPONENTS」、H1「让数据的变化，看得见。」、两行说明（「15 种图表、14 种指标组件、22 种筛选 / 页签 / 表格，一套交互语言。」「悬浮查看读数，点击、滑动与拖拽体验数据变化。」）、右侧统计「15 图表组件 / 14 指标组件 / 22 筛选 · 页签 · 表格」 | H1 42px / 650 / −1.8px 字距（≤700：30px / −1px；≤380：27px）；统计 34px / 450（≤960 隐藏） |
| `.toolbar` | 左：分类筛选胶囊（全部组件 51 · 进度与目标 03 · 时间与趋势 07 · 组成与分类 03 · 结构与策略 02 · 指标与概览 14 · 筛选与查询 11 · 切换与导航 06 · 表格与明细 05；计数为等宽小字）；右：深色 / 浅色切换（按钮前带画布色圆点，组名「图表底色」只在 `aria-label`）、「减少动态效果」复选框、「重播全部」按钮 | 选中筛选 / 选中主题 `aria-pressed=true` → 深绿底 `#242b20` 浅字；≤700px 工具区换行、筛选胶囊隐藏计数 |
| `.collection-meta` | 「THE COLLECTION / N COMPONENTS」（随筛选更新）+ 右侧**截至日胶囊** `#asof-pill`（`role=status`，「数据截至 9/22」；截至日被改动时变琥珀色 `#8a5a00`，文案改为「全页截至 9/14」并跟一个文字按钮「回到最新」）+ 状态点「可交互 · 示例数据」 | 10px 等宽字 |
| `#demo-grid` | 51 个 `.specimen` + 5 条分组分隔标题（`.group-divider`，随筛选显隐；英文眉标 + 中文名 + 右侧一句说明）：KPI CARDS / 指标卡「8 种 · 一张卡回答一个问题；数据同源，派生值实时计算」、METRIC PANELS / 指标面板「6 种 · 多个指标 + 一种组织关系（并列、主次、驱动、对照、链路）」、FILTERS & QUERY / 筛选与查询「11 种 · 筛选只产生条件，读数都从同一份数据现算；单击图表即联动（看板级开关，默认开）」、TABS / 切换与导航「6 种 · 切内容用页签，切参数用分段控件；最多两层」、TABLES / 表格与明细「5 种 · 兜底的明细层：三态排序、行点击可配置、真跨列表头」 | 两列 `repeat(2, minmax(0,1fr))`，间距 26 × 24；`.kpi-wide` / `.ftt-wide` 跨两列；分隔标题通栏、上边线 `#c7cdbe`，标题 15px / 600，说明 11px 右对齐（≤700 换行左对齐） |
| `#tokens` | 01 / DESIGN LANGUAGE「统一底色，让数据成为重点。」（右注「以下为本模板的参考参数，可在正式设计中调整。」）：PALETTE / 色彩角色（深色、浅色两行色板，取自同一套 token，下附三行说明）、HIERARCHY / 阅读层级 4 行（主指标 · 类别与数值 · 辅助标签 · 卡片圆角 / 内边距）、MOTION / 状态过渡 6 行（响应方式 · 数据更新 · 范围切换 · 主题切换 · 指标数字 · 减少动态效果） | 三栏 `1.1fr 1fr 1fr` |
| `#behaviors` | 02 / INTERACTION PRINCIPLES「每一种变化，都有清楚的含义。」：三条原则「01 看见局部，保留整体」「02 直接操作，实时回应」「03 让交互有替代方式」，各配一段说明 | 三栏 |
| `#metrics` | 03 / METRIC RULES「先说清是什么，再说好不好。」：数字格式、涨跌语义、判定三栏（指标卡与指标面板共用，详见 4.0） | 三栏 |
| `#query` | 04 / QUERY RULES「条件从哪来，就回到哪去。」：FILTERS / TABS / TABLES 三栏各 6 条规则（详见 4 · 筛选与查询、切换与导航、表格与明细的共用规则） | 三栏 |
| `.foot` | 「CHART STUDIES / 2026 · 独立交互参考 · 使用固定示例数据」+ 链接「阅读原始设计蒸馏文档」→ 本文件 | |
| `#toast` | 重播全部后的提示「已恢复初始状态并重播全部组件」，1.8s 自动隐藏 | 固定底部居中 |

### 1.2 卡片解剖（`.specimen`）

```
article.specimen[data-group]            ← 筛选分组；data-interacted 标记已操作或已从地址复原
└─ .specimen-shell                      ← --c-surface 底、--c-border 描边、--c-card-shadow，圆角 18，overflow: hidden
   ├─ .card-top (55px; ≤700 50px)       ← 编号(等宽小字) + 中文名 + 英文名 + 「重播」按钮
   └─ .chart-host#chart-<id>            ← 图表容器；padding 18 24（≤960 左右 18，≤700 四边 18）；高度见下文
      ├─ .chart-head (56px)             ← 左：eyebrow + 主指标 / 副标；右：分段控件或单位标签；≤380 高 46px，新增 5 图 52px
      ├─ svg.chart-svg                  ← viewBox 固定坐标系（多为 480×230 / 460×2xx），width 100%
      ├─ 图表自带的说明行 / 图例 / 只读区（各控件不同）
      ├─ .metric-dock                   ← ≤700px 时的指标停靠面板（由 Inspector 注入）
      └─ .sr-only[aria-live]            ← 读屏播报
└─ .card-caption                        ← 手势标签 `.gesture` + 一句话标题 + 说明文字（min-height 74；≤700 65）
```

指标组件的卡片解剖见 4.0 第六条：`.specimen.kpi-spec` 用 flex 让同一行两张卡等高，`.chart-host.kpi-host` 高度由内容决定，不用下面的固定高度（≤700 时仍受下面的 350 下限约束）。

筛选 / 页签 / 表格组件同理：`.specimen.ftt-spec[.ftt-wide]`，`.chart-host.ftt-host` 由内容定高；卡内依次是 `.ftt-bar`（英文眉标 + 中文名 + 右侧说明或控件）→ 控件行 → 结果 / 图表 / 表格 → `.ftt-note` 说明。弹层打开时所在卡片加 `.fk-raised`（z-index 30），`.ftt-spec .specimen-shell` 为 `overflow: visible`，弹层可以越出卡片。

`.chart-host` 高度（按层叠后的实际生效值）：701–1499px 350；≥1500px 375；≤700px（含 ≤380）时 51 张卡都挂了读数层（`.has-inspector`），一律自动高度、最小 350（`!important`），由图与停靠区撑开；`.kpi-host` 同样有这条下限，`.ftt-host` 用 `min-height: 0` 解除。`.chart-svg` 最大高度：≤700 230、701–1499 225、≥1500 245 px，`overflow: visible`；新增 5 图（折线、组合、瀑布、四象限、树状，卡体带 `.extended-chart`）固定 205 px，不随断点变化。（样式表中 ≤700 的 310、≤380 的 280 与 330 / 315 / 335 等早期声明都已被覆盖，清理时可删。）

---

## 2. 设计令牌

### 2.1 页面层（不随图表主题变化）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--paper` | `#EEEDE7` | 页面底色（纸色） |
| `--ink` | `#20231F` | 页面正文 |
| `--muted` | `#73776E` | 页面次要文字 |
| `--line` | `#D6D7CE` | 页面分隔线 |
| `--radius` | `18px` | 卡片圆角 |
| 字体 | `Inter, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`；Kicker / 编号用 `ui-monospace, Consolas` | 数字统一 `tabular-nums` |
| 焦点环 | 页面元素 `#7CAC85` 2px；SVG 内 `[tabindex]` 元素 `--c-focus` 2px，但读数目标（`[data-metric-target]`）不画轮廓、改用 `--c-focus-soft` 光晕，探查区 SVG 为 1px `--c-focus-line` 虚线；指标 / 筛选组件的读数目标为 2px `--c-focus-soft` 描边；目标线把手 `--s-gold-text` 3px | `:focus-visible` |

页面层的筛选、工具条、说明文字、设计规范区等仍写字面色值（它们永远落在纸色上），只有卡片与读数浮层走图表主题。

### 2.2 图表主题 token（深色 / 浅色）

**结构**：`:root, [data-chart-theme="dark"]` 定义深色（默认），`[data-chart-theme="light"]` 定义浅色；属性写在 `<html>` 上即全局生效，写在任意元素上则只作用于其子树（设计规范区的两行色板就是这样同时展示两套色）。命名分四组：`--c-*` 结构与文字、`--s-*` 系列色、`--seq-*` 热力序列、`--st-*` 状态色。共 102 个：结构与文字 31、系列 55（17 个系列 × 色块 / `-on` / `-text`，另有超额、徽标、根节点次级文字 4 个）、热力 5、状态 9（3 种状态 × 色块 / `-text` / `-soft` 胶囊底），外加两个投影。

**深色 = 基准原值**：每个 token 的深色值取自基准版实际用色；基准中同一角色的近似灰值（如 13 个轴标签灰）合并为一个 token，合并前后最大色差 ΔE 4.2（OKLab×100，集中在 10px 以下的卡头小字），逐卡像素比对平均差 ≤0.05/255。

**浅色推导原则**（逐项选定，不做反相）：
1. 结构与文字按"同角色、同对比档位"重建：主文字 15.3:1，次级 8.8:1，标签 6.4:1，轴标签 5.1:1，弱化 3.1:1（均以浅色画布 `#fbfbf8` 计）。
2. 系列色保持色相家族，按 OKLCH 把明度压到 0.53–0.79、适度提饱和；再用 dataviz 校验脚本逐图检查相邻色（CVD ≥8、正常视觉 ΔE ≥15），气泡与四象限按全部两两组合检查。
3. 同一图里要分开的低饱和类别（鼠尾草绿 / 橄榄 / 中性）靠明度拉开：绿 0.665、橄榄 0.525、中性 0.895；因此橄榄在浅色里更接近古铜色。
4. 中性类别在两套主题里都贴近纸色（深色米白、浅色浅灰），Bike 深灰气泡在两套主题里都是深色——气泡与占比条换主题后配色关系不变。
5. 深色里的"白色系列"（圆环 EXERCISE）拆出 `--s-ink`：深色白、浅色墨；选中分段、目标标签与筛选 / 页签的选中态统一为反色 `--c-inverse`（深色浅胶囊、浅色深胶囊）。
6. 文字不借用浅色系列色：凡系列色被当作文字或焦点描边（圆环百分比、仪表 NORMAL、象限标签、焦点环），改用 `-text` 变体（浅色 ≥4.6:1）；色块上的文字用 `-on` 变体（按亮度取墨或白）。

#### 2.2.1 结构与文字

| Token | 深色 | 浅色 | 用途 | 浅色对比度 |
|---|---|---|---|---|
| `--c-surface` | `#111214` | `#fbfbf8` | 卡片画布 |  |
| `--c-control` | `#27292e` | `#ecebe5` | 分段控件轨道、按钮底 |  |
| `--c-panel` | `#202326` | `#ffffff` | 读数浮层；筛选弹层 / 抽屉 / 页签菜单底（窄屏停靠区改用 `--c-inset` 底、`--c-outline` 描边） |  |
| `--c-inset` | `#191d1f` | `#f3f2ed` | 卡内嵌入区（窄屏停靠面板） |  |
| `--c-hover` | `#2b3026` | `#efeee8` | 卡头按钮悬停底 |  |
| `--c-border` | `#292b2b` | `#dddcd3` | 卡片描边 |  |
| `--c-divider` | `#242629` | `#ebeae3` | 卡头分隔线 |  |
| `--c-outline` | `#363a31` | `#d3d2c9` | 卡内小控件 / 标签描边 |  |
| `--c-panel-border` | `#42474b` | `#dcdbd2` | 面板描边 |  |
| `--c-panel-rule` | `#393f40` | `#edece6` | 面板内分隔线 |  |
| `--c-track` | `#2b2d31` | `#e8e7e0` | 轨道、基线、未选中柱 |  |
| `--c-grid` | `#292c30` | `#e8e7e1` | 网格线 |  |
| `--c-baseline` | `#383a3f` | `#c9c8bf` | 坐标基线 |  |
| `--c-connector` | `#4d5748` | `#c3c9bb` | 阶梯连线、树节点描边 |  |
| `--c-rule` | `#61695c` | `#a6ada1` | 分界虚线、树连线 |  |
| `--c-leader` | `#8a8e86` | `#999d94` | 瀑布桥接线 |  |
| `--c-text` | `#f1f0e9` | `#20231f` | 主文字、指针、游标 | 15.3:1 |
| `--c-text-2` | `#c0c4bd` | `#454a43` | 次级文字、图例名 | 8.8:1 |
| `--c-text-3` | `#a5aaa5` | `#585e56` | 标签、控件文字 | 6.4:1 |
| `--c-text-4` | `#979b9f` | `#676d64` | 轴标签、眉标、单位 | 5.1:1 |
| `--c-text-5` | `#85868f` | `#8e9289` | 弱化文字（未选中图例） | 3.1:1 |
| `--c-inverse` | `#f1f0e9` | `#262a23` | 反色：选中分段、目标标签、筛选 / 页签选中态（chip、主按钮、开关、日历选中日、药丸页签） |  |
| `--c-on-inverse` | `#1b1f21` | `#f6f5ef` | 反色胶囊上的文字 | 13.4:1 |
| `--c-dim` | `#3d3e43` | `#dfded7` | 未选中扇段 |  |
| `--c-dim-dot` | `#64656b` | `#c7c7c0` | 未选中图例色点 |  |
| `--c-off` | `#363940` | `#d9d8d0` | 未达标柱 |  |
| `--c-focus` | `#f1f0e9` | `#20231f` | 键盘焦点环 | 15.3:1 |
| `--c-focus-soft` | `#a5b7a8` | `#5f7a65` | 图形焦点光晕 | 4.5:1 |
| `--c-focus-line` | `#819183` | `#7f9483` | 探查区域焦点虚线 |  |
| `--c-node-1` | `#2b322a` | `#eef1ea` | 树状图分支节点 |  |
| `--c-node-2` | `#202429` | `#f6f6f2` | 树状图叶节点 |  |
| `--c-shadow` | `0 8px 30px #0005` | `0 10px 28px rgba(32,35,31,.14), 0 1px 3px rgba(32,35,31,.08)` | 浮动面板投影 | |
| `--c-card-shadow` | `none` | `0 1px 2px rgba(32,35,31,.05)` | 卡片投影 | |

#### 2.2.2 系列色（每个系列 3 个 token：色块 / 色块上的文字 `-on` / 用作文字或焦点描边 `-text`）

| 系列 | 深色 色块 | 浅色 色块 | 浅色 `-on` | 浅色 `-text` | 浅色色块对比 | 用途 |
|---|---|---|---|---|---|---|
| `--s-gold` | `#eac34d` | `#d2ab17` | `#17181b` | `#896e01` | 2.1:1 | 金黄：圆环 MOVE、占比 Focus、毛利率、现金牛 |
| `--s-sage` | `#b8c7ab` | `#6fa374` | `#17181b` | `#4a7d50` | 2.8:1 | 鼠尾草绿：STAND、NORMAL、营收、正向、明星、根节点 |
| `--s-blue` | `#7baed0` | `#4188b6` | `#17181b` | `#2f77a4` | 3.7:1 | 雾蓝：柱状图、折线、期初期末 |
| `--s-purple` | `#baa1d2` | `#8463a8` | `#ffffff` | `#8463a8` | 4.7:1 | 淡紫：面积图、成本、问题 |
| `--s-orange` | `#e78b5f` | `#cf5b28` | `#17181b` | `#c14e18` | 3.9:1 | 橙：达标柱、负向贡献、瘦狗 |
| `--s-mint` | `#74cfb7` | `#269b7e` | `#17181b` | `#048167` | 3.3:1 | 薄荷：与 `--seq-4` 同值（热力格最高档实际取 `--seq-4`）；本身只被已隐藏的热力格旧提示框引用 |
| `--s-coral` | `#de656d` | `#c94750` | `#ffffff` | `#c7454f` | 4.5:1 | 珊瑚：仪表 NORMAL 区 |
| `--s-lime` | `#c2d965` | `#b4c82b` | `#17181b` | `#6c7900` | 1.8:1 | 黄绿：DEEP 深度专注 |
| `--s-pink` | `#de88a4` | `#ad507a` | `#ffffff` | `#ad507a` | 4.8:1 | 粉：Video |
| `--s-lavender` | `#c2bbf3` | `#8379ca` | `#17181b` | `#7267b6` | 3.6:1 | 浅紫：Walk |
| `--s-olive` | `#aaa77c` | `#7a6939` | `#ffffff` | `#7a6939` | 5.2:1 | 橄榄：第三类别 |
| `--s-neutral` | `#efeee4` | `#dedcd7` | `#17181b` | `#73716d` | 1.3:1 | 中性：第四类别（两主题都贴近纸色） |
| `--s-ink` | `#f1f0e9` | `#3e413a` | `#ffffff` | `#3e413a` | 10.0:1 | 墨色：圆环 EXERCISE（深色为白，浅色为墨） |
| `--s-graphite` | `#55555c` | `#3d3c38` | `#ffffff` | `#3d3c38` | 10.7:1 | 深灰：Bike（两主题都为深色气泡） |
| `--s-slate` | `#85888c` | `#babec3` | `#17181b` | `#6e7276` | 1.8:1 | 石板灰：BREAK 休息 |
| `--s-band-low` | `#42464b` | `#e2e1dd` | `#17181b` | `#72716e` | 1.3:1 | 仪表 LOW 区 |
| `--s-band-high` | `#5b5f65` | `#b7bbc0` | `#17181b` | `#6e7277` | 1.9:1 | 仪表 HIGH 区 |

深色主题中 `-on` 统一为 `#17181b`（`graphite`、`band-low`、`band-high` 三个深灰为 `#f0f0ed`）；`-text` 除这三个深灰外都等于色块本身，三个深灰的色块在深底上只有 2.0–2.9:1，`-text` 另取提亮值：`--s-graphite-text` `#7d7d84`、`--s-band-low-text` `#787d82`、`--s-band-high-text` `#797d84`。全部 `-text` 在深底上 ≥4.5:1。

#### 2.2.3 热力格序列 · 状态色 · 其它

| Token | 深色 | 浅色 | 用途 |
|---|---|---|---|
| `--s-gold-deep` | `#927127` | `#93690d` | 圆环超额段 |
| `--s-gold-soft` | `#51421a` | `#f6e7b6` | OVER GOAL 徽标底 |
| `--s-gold-soft-ink` | `#edda93` | `#6f5000` | 徽标文字、聚焦模式提示 |
| `--s-sage-on-2` | `#435039` | `#1a2e1f` | 根节点上的次级数值 |
| `--seq-0` | `#2b2d31` | `#e8e7e0` | 热力 0 次（=轨道） |
| `--seq-1` | `#565c50` | `#ced0bd` | 热力 1 次 |
| `--seq-2` | `#93987b` | `#a6a78b` | 热力 2 次 |
| `--seq-3` | `#d4d6bf` | `#797c5d` | 热力 3 次 |
| `--seq-4` | `#74cfb7` | `#269b7e` | 热力 ≥4 次（=薄荷） |
| `--st-good` | `#4cc38a` | `#18a058` | 状态：向好（指标卡涨跌、正常） |
| `--st-good-text` | `#4cc38a` | `#03793f` | 状态文字：向好 |
| `--st-good-soft` | `#1a2e27` | `#e0f0e5` | 状态胶囊底：向好 |
| `--st-bad` | `#f2716a` | `#e64c45` | 状态：向差（指标卡涨跌、异常；时间进度虚线） |
| `--st-bad-text` | `#f2716a` | `#c62b2a` | 状态文字：向差 |
| `--st-bad-soft` | `#352122` | `#f8e6e3` | 状态胶囊底：向差 |
| `--st-warn` | `#f0b43f` | `#d9920a` | 状态：关注（滞后、阈值中段） |
| `--st-warn-text` | `#f0b43f` | `#936100` | 状态文字：关注 |
| `--st-warn-soft` | `#352c1b` | `#f7eedb` | 状态胶囊底：关注 |

状态文字色的取值规则比系列色多一道：它经常写在**自身 12–16% 的淡色胶囊底**上，因此 `-text` 同时满足「对画布 ≥4.6:1」与「对 `-soft` 胶囊底 ≥4.6:1」（浅色实测 5.1–5.4 / 4.6，深色 6.6–10.1 / 5.3–7.4）。2026-09-23 因此把浅色三个 `-text` 各加深一档（见 8.6）。

**浅色校验结果**（`validate_palette.js --mode light --surface #fbfbf8`）：

| 图 | 检查的颜色组 | 最差 CVD ΔE | 最差正常视觉 ΔE |
|---|---|---|---|
| 分段占比条 | gold · sage · olive · neutral（相邻） | 10.5 | 15.2 |
| 环形占比图 | pink · sage · olive · neutral（相邻） | 11.2 | 16.0 |
| 组合图 | sage · purple · gold（相邻） | 17.2 | 22.1 |
| 瀑布图 | blue · sage · orange（相邻） | 8.6 | 15.1 |
| 专注分段图 | slate · sage · lime（相邻） | 13.4 | 16.2 |
| 活动圆环 | gold · ink · sage（全部两两；相邻两环 gold–ink、ink–sage 为 29.9 / 30.6） | 10.5 | 15.2 |
| 四象限图 | sage · purple · gold · orange（全部两两） | 8.6 | 15.2 |
| 气泡图 | lavender · sage · graphite · olive · neutral（全部两两） | 13.9 | 16.0 |

全部通过 CVD（≥8）与正常视觉（≥15）两道门槛。脚本另有两项判 FAIL（运行结果为 FAILED），均为有意取舍、已由直接标签 / 图例 / 缝隙补偿：明度带（浅色 0.43–0.77）之外有中性 0.895、石板灰 0.800、黄绿 0.791（偏亮）与墨 0.370、深灰 0.356（偏暗）；色度低于 0.10 的有鼠尾草绿 0.088、橄榄 0.070、雾蓝 0.0999（临界），以及中性、石板灰、墨、深灰四个灰阶。另有 WARN：金黄、鼠尾草绿、黄绿、石板灰、中性在浅底上低于 3:1，所在图表均有直接标签。深色沿用基准，低饱和三色的相邻色差偏小（约 10），靠标签与缝隙区分。

### 2.3 色彩角色（`#tokens` 区块公布的六色，两套主题同时展示）

| 角色 | Token | 深色 | 浅色 | 承担 |
|---|---|---|---|---|
| 画布 | `--c-surface` | `#111214` | `#fbfbf8` | 承载数据 |
| 金黄 | `--s-gold` | `#EAC34D` | `#d2ab17` | 圆环 MOVE、占比条 Focus、组合图毛利率、矩阵现金牛、树状图选中描边 |
| 鼠尾草绿 | `--s-sage` | `#B8C7AB` | `#6fa374` | 圆环 STAND、专注 NORMAL、占比条 / 环形 / 气泡第二类、组合图营收、瀑布正向、矩阵明星、树根节点 |
| 雾蓝 | `--s-blue` | `#7BAED0` | `#4188b6` | 柱状图、折线图、瀑布期初 / 期末 |
| 淡紫 | `--s-purple` | `#BAA1D2` | `#8463a8` | 面积图、组合图成本、矩阵问题 |
| 橙 | `--s-orange` | `#E78B5F` | `#cf5b28` | 目标线达标柱、瀑布负向、矩阵瘦狗 |

其余系列色见 2.2.2。**规则**：原 10 图每张卡片只有一个饱和主色，其余分类退回鼠尾草绿 / 橄榄 / 中性 / 灰阶；组合图、瀑布图、四象限图与 K05 按系列需要用 2–4 个系列色，均配直接标签或图例。选中态同时用颜色、位置与文字表达，不靠颜色单独承担。

### 2.4 阅读层级

| 层级 | 规格 | 示例 |
|---|---|---|
| 主指标 `.metric` | 28px / 600 / −0.9px 字距（≤700：26px） | `46 MIN` `8.8K` |
| 眉标 `.eyebrow` | 10px / 500 / 1.5px 字距 / `--c-text-4` | `ACTIVITY RINGS` `DAILY AVG · 日均专注` |
| 新增 5 图 `.extended-chart` | 主指标 `.ext-metric b` 27px / 600 / −0.7px，副标 10px `--c-text-3`；眉标 9px / 1.1px 字距 | `240 总营收 · 万元` |
| 类别与数值 | 12–15px（图例名 14–14.5、图例数值 15 / 650；气泡名 14 / 650、数值 12） | 图例名、气泡名；树节点另为 11px（数值 10px） |
| 辅助标签 `.unit` `.axis-label` | 10–11px / `--c-text-4` `--c-text-3` | `MIN` `3 DAILY GOALS` 轴刻度 |
| 卡片说明 `.card-caption` | 标题 12px / 600；正文 11px / 1.65 行高；手势标签 10px 描边小方标（圆角 3px） | |
| 分段控件 `.segmented` | 10px / 20px 圆角胶囊，轨道 `--c-control`，选中为反色胶囊 `--c-inverse` | WEEK / MONTH · 1W / 1M / 1Y |
| 读数浮层 | 标题 12px / 600；行 11px；脚注 10px | |
| 指标卡 | 主数值 28px / 600（面板主指标 42px，三联 / 宫格 22–24px，页签 20px）；眉标 10px + 中文名 11px / 550；对比与脚注 10–11px；状态胶囊 22px 高 | 见 4.0 |
| 窄屏下限 | SVG 文字在 ≤700px 时按缩放比换算，屏幕上不小于 8.5px（`fitLabels`） | |

### 2.5 状态过渡

| 项 | 基准 |
|---|---|
| 响应方式 | 就地反馈：面板贴着指针 / 元素出现，读数在固定位（顶部、中心、图下） |
| 数据更新 | 图形与数字同步：主指标随动画插值（`mix(from, to, p)`） |
| 范围切换 | 保留空间参照：同一坐标系内插值到新数据，不清空重画；切换后清除旧选择 |
| 进场时长 | 780–1450ms（专注分段图无进场；各控件见第 4 节）；切换 540–850ms（树分支收起 / 展开 350ms）；选中 300–330ms（气泡聚焦为 1500ms 物理运动） |
| 缓动 | 默认 `1−(1−t)³`（ease-out cubic）；仪表盘用回弹 `1+(c+1)u³+cu²`（c=0.8）；热力格进场、气泡聚焦、树状图横纵切换为线性 |
| 主题切换 | 颜色 320ms 过渡（切换期间给 `<html>` 加 `.theme-switching`），图表状态（选中、范围、游标、目标值）全部保留；减少动态效果时直接切换 |
| 指标数字 | 进场从 0 计数 700ms（K04、P02 为 800ms；K06 从色带下限起计，800ms）；周期 / 口径切换在两个值之间插值 500–650ms，不清空重画；计数期间锁定终值宽度，单位不滑动 |
| 减少动态效果 | 复选框与 `prefers-reduced-motion` 双向同步；开启后 `animate()` 直接 `update(1)`，CSS 过渡压到 0.01ms，但操作反馈保留 |

---

## 3. 交互系统

### 3.1 `ChartKit`（共享原语）

| API | 说明 |
|---|---|
| `s(tag, attrs, parent)` | 创建 SVG 元素；`text` 键写入文本；`fill / stroke / stop-color` 等颜色键若是 `var(--token)` 写进内联 style（主题切换即时重绘），`none / transparent` 仍写属性（选择器依赖它） |
| `paint(el, prop, value)` | 动态改颜色的唯一入口，规则同上；图表里不再直接 `setAttribute("fill", …)` |
| `tone(color, kind)` | 由系列色取配套 token：`tone("var(--s-sage)", "text")` → `var(--s-sage-text)`，`"on"` → `var(--s-sage-on)`；非系列色原样返回 |
| `animate(key, duration, update, ease)` | 以 `key` 登记的补间；同 key 再次调用会取消前一个；`reduced()` 或 `duration ≤ 0` 时立即 `update(1)`（后者用于瞬时置位）；启动时先同步 `update(0)`。帧时间已夹紧到 `[0,1]`（见 8.1） |
| `scope(root, fn)` / `finish(root)` | 记录动画归属的卡片；用户在该卡触发读数（悬浮 / 聚焦 / 按下数据项，即读数层的 `settle()`）时，该卡名下未完成的补间（含与进场共用 key 的切换）直接跳到终态，避免"边进场边交互"；点卡内普通按钮后重新生效 |
| `arc(cx, cy, r, start, end)` | 圆弧路径，角度以 12 点为 0°、顺时针 |
| `onKey(el, fn)` | Enter / Space 触发 |
| `reduced()` / `setReduced(v)` | 动效开关；给 `<html>` 加 `.reduce-motion`；开启时进行中的补间立即跳到终态 |
| `clamp(n, a, b)` | |

### 3.2 进场编排

- 15 个图表与 13 个指标组件（P05 除外）：`reset()` = 恢复初始状态 + 播放进场动画，构造时立即播一次进场（多数直接调用 `reset()`）；专注分段图没有进场动画，`reset()` 只把游标放回 16:14。
- 22 个筛选 / 页签 / 表格组件与 P05：构造时直接渲染（筛选、页签先读自己的地址键），不调 `reset()`；`reset()` 恢复默认状态（筛选、页签同时用 `replaceState` 清掉对应地址键），不播进场。
- `IntersectionObserver`（阈值 0.12）：卡片首次进入视口时若没有 `data-interacted`，执行一次「重播」，保证用户看到完整进场；之后不再观察。`data-interacted` 在卡内 `pointerdown` / `keydown` 时写入；筛选 / 页签卡带着自己的地址键时（F11 还包括截至日不是最新时）构造时也写入，复原的状态不会被进场重播冲掉。
- 「重播」= `inspector.reset()` + `instance.reset()`；「重播全部」遍历所有实例并弹 toast。

### 3.3 `ChartInspector`（读数浮层）

每个控件通过 `root.inspector` 提供**数据适配器**，读数层负责一切呈现。页面文案统一叫「读数浮层」，与新增的「指标面板」（P01–P07）区分开：

```js
root.inspector = {
  targets: [{ el, read: () => ({ title, rows: [[label, value, color?]], foot? }), drag?: true }],
  probe:   { svg, min, max, top, bottom, count(), read(i), current?(), hover?(i), guide?: false },
  lookup:  el => ({ el, read }) | null,   // 动态目标：随渲染重建的行、条形、色段，按需查找
  legacy:  [旧版提示元素],  // 会被隐藏，保证只有一套提示层
  placeholder: "轻点图形查看读数"   // 窄屏停靠区占位文案，可按控件改写
};
```

`ChartInspector.mount(root)` 在控件构造完成后调用，约定如下：

- **必须有适配器**：没有读数的控件也要给 `root.inspector = {targets: []}`，否则 mount 抛错，卡片显示「这个组件没能加载」。
- **每卡一条播报通道**：mount 在控件根末尾建一个 `.sr-only` 的 `role=status`（`aria-live=polite`）播报区，并把控件内其它已有的 `aria-live` 一律改为 `off`。控件要播报就写进这个播报区（FTT 组件的 `say(root, msg)`、`TableKit`、`FilterKit.cal` 都这样做）；15 个原有图表自带的切换 / 选中播报区因此不发声。
- **自动补 ARIA**：目标没有 `tabindex` 补 `0`，没有 `aria-label` 补 `read().title`；probe SVG 没有 `tabindex` 时补 `tabindex=0` 与 `role=group`，`aria-label` 末尾加「左右方向键选择采样点」；当前目标写 `aria-describedby`，指向浮动面板（`role=tooltip`），窄屏指向停靠区。
- **首次交互**时立即结束本卡进场动画（`ChartKit.finish`）并写 `data-interacted`。

| 行为 | 规则 |
|---|---|
| 悬浮 | 指针进入 `targets` 元素或 `probe` 区域 → 100ms 后显示浮动面板 `.metric-panel`（244px，贴指针右上，超出卡片时翻转，卡片滚出视口时隐藏）；已有面板正在显示、或上一个面板关闭不到 300ms 时，下一个直接出现，不再等 100ms |
| 移开 | 指针移到图形之间的空隙或卡内空白：面板**继续跟随指针**，保留上一条读数；120ms 宽限期从离开目标起**只计一次**，之后的移动不顺延；宽限期内进入下一个目标直接换读数，否则关闭——已有固定项（触屏 / 窄屏点选）时不关闭，回到固定的那条读数。移出图表区或焦点移出控件同样 120ms 后处理 |
| 探查（probe） | 指针在 `top…bottom` 之间、x 在 `min − 18 … max + 18` 内时，x 按 `min…max` 线性映射并取整到最近采样点 `i`；`min / max / top / bottom / count()` 每次现读，可写成 getter 跟随真实像素宽或截至日；采样点变化时调 `hover(i)`，离开时调 `hover(-1)`；显示虚线导引线（`guide:false` 可关）。同一 SVG 里也有 targets 时，指针优先按 probe 取样，键盘焦点优先读 target。使用者：面积图、折线图、组合图（`guide:false`）、专注图、P02 / P03（`guide:false`）、T01 趋势页 |
| 点选固定 | 鼠标（宽屏）：点击只让面板立即出现，不固定，移开按上条关闭；触屏 / 窄屏：点一下即固定；宽屏触屏点图表外任意处取消固定 |
| 拖拽锁定 | `drag:true` 或 `role=slider` 的目标在按下期间锁定面板并跟随读数；松手后触屏 / 窄屏固定，鼠标回到悬浮规则 |
| 键盘 | 焦点进入目标 → 立即显示面板，并向本卡播报区播报「标题，标签 数值，…，脚注」（只在键盘操作且内容变化时）；probe 型 SVG 没有 `current()` 时由读数层处理 ← → Home End 选采样点，有 `current()` 的（专注图游标）按键由控件自己处理、读数层按 `current()` 重读；在目标上按方向键、Home / End、PageUp / PageDown、Enter、Space 后都重读当前目标 |
| 窄屏（≤700px） | 浮动面板禁用，改为图下 `.metric-dock`（占位文案取 `placeholder`，默认「轻点图形查看读数」，最小高 164px） |
| 取消 | Esc；点击控件内不是读数目标的按钮（分段控件等）；宽屏下在当前图表外按下指针；`pointercancel`；窗口 resize（收起浮动面板，窄屏固定项保留）；卡片滚出视口（只隐藏）；切换筛选时对隐藏卡片派发 `metric-hide`（FilterKit 弹层、F08 抽屉也监听它并关闭） |
| 面板内容 | 标题 + 若干「标签 / 数值（可带色点）」行 + 可选脚注（如口径说明）；`read()` 返回 `null` 时不显示；数值为 `null` / `undefined` / 非有限数时写「暂无数据」 |
| 动态目标 | 表格行、条形等会随渲染重建：元素带 `data-metric-target`，读数层先查静态 `targets`，查不到再调 `lookup(el)`；当前目标被移出 DOM 时面板自动关闭，不会停在旧数据上 |
| 移动开销 | 面板用 `transform: translate3d()` 移动（`will-change: transform`，独立合成层），移动时不触发布局、不重绘下面的图表；位置取整后与上次相同就不写；导引线、`aria-describedby`、`data-interacted` 等只在变化时写；窄屏判断复用同一个媒体查询对象 |

### 3.4 选中与降噪约定

| 控件 | 触发 | 被选项 | 其余项 | 取消 |
|---|---|---|---|---|
| 柱状图 | 点击 / Enter / Space | 原色；读数走读数浮层（柱顶白色胶囊作为 `legacy` 隐藏） | 退为轨道色 `#2B2D31` | 再点 / Esc / 切换范围 |
| 分段占比条 | 点击段或图例 | 上抬 14px（300ms）；读数走读数浮层（顶部胶囊与 1px 引线作为 `legacy` 隐藏） | 保留各段颜色 | 再点 / Esc |
| 环形占比图 | 点击扇段或图例 | 外移 10px，中心换为「26% / Reading」 | 退为 `#3D3E43`，图例变灰 | 再点 / Esc |
| 气泡图 | 点击气泡 | 放大居中 r=75，白色描边 | 缩到 0.64 倍并让位到固定槽位 | 再点 / Esc / 「返回比例」 |
| 折线图 | 悬停 / 聚焦数据点：导引虚线 + 说明行；点击 / Enter / Space 选中 | 选中点 r4→6 + 白描边 2px，导引虚线停在选中点 | 不变 | 再点 / Esc |
| 组合图 | 悬停 / 点选月份 | 该月柱不变，指标切换为当月 | 其他月透明度 .28 | 再点 / Esc / 切换半年 |
| 瀑布图 | 点选阶段 | 不变 + 说明行明细 | 透明度 .35 | 再点 / Esc / 切换实际 · 预算 |
| 四象限图 | 点选气泡 | 白描边 2px + 说明行 | 透明度 .3 | 再点 / Esc（切换季度不清除，见 3.5） |
| 树状图 | 点根 / 叶 | 金黄描边 + 占比说明 | 不变 | 再点 / Esc |
| 树状图（分支） | 点中间层节点 | 收起 / 展开子项（350ms，符号 − / +，`aria-expanded` 同步），同时选中该分支：金黄描边 + 占比说明 | 其它分支槽位不动 | 再点只切换收起 / 展开，选中保留；Esc 取消选中 |

### 3.5 范围 / 视图切换约定

分段控件 `.segmented button[aria-pressed]`；切换时：**范围切换**（柱状 周 / 月、组合 上 / 下半年、瀑布 实际 / 预算）先清除选择，**形态 / 方向切换**（折线 折 / 曲、树状图 横 / 纵）保留选中项（树状图同时保留收起状态），矩阵 Q1 / Q2 也保留选中项（选中气泡随季度迁移，说明行改读新季度）→ 在同一坐标系内插值（位置、宽度、高度、透明度、主指标一起 `mix`）→ 更新轴标签与说明行。播报只走本卡播报区（见 3.3「每卡一条播报通道」）：筛选 / 页签 / 表格组件的切换写进播报区；15 个原有图表自带的切换播报区被静音，指标组件也不另行播报，所以这两组的切换不播报。时长：柱状 540ms、面积 620ms、折线 650ms、树状图方向 650ms（经不可见中点淡出淡入，避免节点交叉飞行）、组合 / 瀑布 750ms、矩阵 850ms（份额按对数插值）、仪表盘示例 850ms。

### 3.6 直接操作约定

| 控件 | 指针 | 键盘 | 范围 / 步长 |
|---|---|---|---|
| 专注分段图（时间游标） | 按下即跳到该处，横向拖动跟随；`touch-action: pan-y` 不挡页面竖向滚动 | ← ↓ −1、→ ↑ +1 分钟；Shift 时步长 15；PageUp / PageDown ±60；Home 09:00 / End 17:00 | 09:00–17:00（0–480 分钟） |
| 目标线 | 整条线 + 把手为可拖区（`role=slider`，`touch-action:none`），纵向拖动 | ↑ ↓ ← → ±100 步；PageUp/Down ±1000；Home 2000 / End 10000 | 2,000–10,000 步，按 100 取整 |

### 3.7 图表主题机制

| 环节 | 实现 |
|---|---|
| 状态位置 | `<html data-chart-theme>`，取值 `dark` / `light`，默认深色。用独立属性名，避免与宿主（如 Claude 预览）写在 `<html>` 上的 `data-theme` 冲突 |
| 首帧无闪烁 | `<head>` 内一小段脚本在渲染前读取 `localStorage["chart-studies-theme"]` 并写属性；读写都包在 try/catch 里，存储不可用时回落深色 |
| 切换 | 工具条「图表底色」两按钮（`aria-pressed`）；切换只改属性，所有颜色经 CSS 变量即时重绘，图表不重建、状态不丢 |
| 过渡 | 切换瞬间给 `<html>` 加 `.theme-switching`（420ms 后移除），期间卡片与面板的颜色类属性统一 320ms 过渡；减少动态效果时跳过 |
| 作用域 | 两套 token 都挂在属性选择器上，可在任意子树局部指定主题（设计规范区两行色板即用此法；以后可做单卡片强制浅色） |
| 浮动面板 | `.metric-panel` 挂在 `<body>` 下，跟随全局主题；窄屏停靠区在卡内，跟随卡片 |
| 焦点优先级 | 颜色改为内联 style 后会压过普通 CSS，扩展图表的焦点描边规则加 `!important` 以保持原有焦点表现 |

### 3.8 `KpiKit`（指标原语）

指标卡与指标面板共用的第二套原语，只负责格式、判定与几种通用图形；颜色、动画仍走 `ChartKit`。

| API | 说明 |
|---|---|
| `fmt(v, kind, opts)` → `{num, unit}` | `kind`：`amount`（存万元；万 1 位 / 亿 2 位）/ `yuan` / `count` / `rate` / `hours`，其它值按普通数字；`opts.scale` 钉住单位，`short` 去掉「元」，`unit` 给计数加量词（「4.8 万单」），`d` 指定 `rate`（默认 1 位）与普通数字的小数位；`v` 缺失或非有限数时返回 `{num:'—', unit:''}`；负号用 U+2212，舍入为 0 时不带负号 |
| `text(v, kind, opts)` | `fmt` 的字符串形式 |
| `MINUS` / `nf(v, d)` | U+2212 负号；千分位格式化（取绝对值，符号由调用方加） |
| `cardScale(kind, ...values)` | 按卡内最大值选单位，保证一张卡里金额单位统一 |
| `change(cur, base, {change, polarity})` | → `{raw, shown, flat, up, tone, unit, arrow, text, dir, verdict}`；基期缺失、非有限，或 `ratio` 的基期为 0 时返回 `null`（`deltaHTML` / `setDelta` 画「—」）；`change` 取 `ratio` / `pp`；`polarity` 取 `up` / `down` / `neutral`，`tone` ∈ `good / bad / flat`（`neutral` 只标方向）；`shown` 绝对值 < 0.05 为持平 |
| `fromRaw(raw, mode, polarity)` | 已知变化值时构造同一结构（动画中途按当前值着色） |
| `deltaHTML(d)` / `setDelta(el, d)` | 涨跌片段的生成与就地更新 |
| `pace(rate, time)` | 超前 / 持平 / 滞后，取卡面显示值相减 |
| `band(v, [lo, hi], polarity)` | 阈值判档，按原始值 |
| `chipHTML / setChip / statusChip` | 状态胶囊 |
| `STATUS` | `good / warn / bad` → 图标 ✓ ! ✕ + 文字 正常 / 关注 / 异常 |
| `count(C, key, items, dur)` | 数字计数（`items` 项为 `{el, from?, to, format}`，`dur` 默认 700）；先写终值量出宽度并锁 `min-width`，动画结束释放 |
| `spark(C, svg, o)` → `{draw, progress(p), clear()}` | 迷你图（`kind`：`area` / `line` / `bars`）：按真实像素宽重绘，自带指针 / 键盘探查（← → Home End，Esc 清除，按键 `stopPropagation`）、`role=slider`、`aria-valuetext`；`o` 另有 `values`、`color`、`name`、`valueText(i)`、`onScrub(i)`（离开时传 `null`）、`partialLast`（未满周期样式）、`height`（默认 44） |
| `canvas(svg, draw)` | 宽度变化超过 0.5px 时按真实像素宽调 `draw(W)`，返回 `{width(), redraw()}`；没有组件使用它——P02 / P03 各自用 `ResizeObserver` 重绘 |
| `roving(items, onMove)` | 组内方向键移动 + 单一 Tab 停靠点；移动后 `stopPropagation`，避免读数层重读旧目标 |
| `niceDomain(lo, hi, n)` → `{min, max, step}` | 坐标域取整 |

数据与格式规则见 4.0；`window.KpiData` 暴露 `DATA / SC / MET / scAt` 便于在控制台核对派生值（`scAt(day)` 按任一截至日重算本月 / 本季 / 本年）。

### 3.9 页面级状态：`Hash` 与 `AsOf`

筛选 / 页签 / 表格三组共用两件页面级的东西：地址里的状态，和全页唯一的截至日。

| 项 | 规则 |
|---|---|
| 地址格式 | `#!k=v&k=v`，用 `URLSearchParams` 序列化：值里的 `,` `~` `=` `;` 在地址中写成 `%2C` `%7E` `%3D` `%3B`（手写不转义的链接也能读）；键 = 组件编号小写：`f01`–`f11`、`t01 t02 t05 t06 t07 t08`、`asof`。**默认状态不写**，只写偏离默认的部分，例如 `#!f01=d7&t01=mix&asof=0914&f04=east%2Ccentral&t08=online` |
| 值的写法 | 以下为解码后的值，尽量短且可读：`f01=d7` / `c0919-0920`（自定义区间）；`f02=0901-0922.yoy`；`f06=direct~repeat`；`f08=r=east,south;v=50-210;g=up`；`f09=t=d7;g=east;lr=east;lc=online`（关闭联动写 `x=0`，此时不带 `lr / lc`）；`f10=central~40`；`f11=0914`；`t08=` 视图 id。F04 / F05 / F07 / F08 / F09 的多选按固定顺序编码与比较（大区按 REG 顺序、区县按叶子顺序），勾选先后不影响地址，勾回默认项不写 |
| 写入 | `Hash.set(k, v, {push})`（`push` 默认 true）：用户操作走 `pushState`（800ms 内的连续操作合并为一条历史，后退一次回到上一步），重置与复原走 `replaceState`；`v` 为 null / undefined / 空串即删键，与现值相同不写；Hash 写入的历史条目与页面初始条目都带 `history.state` 标记 `{chartHash:1}`；表格的排序、翻页、勾选不写地址 |
| 读出 | 组件构造时用 `Hash.get(k)` 读自己的键，用 `Hash.on(k, fn)` 订阅：导航时回调 `fn(值)`，键不在地址里时传 `undefined`，即回默认。`popstate` / `hashchange` 时，当前条目带 `chartHash` 标记或哈希以 `#!` 开头才通知 —— 所以后退 / 前进到「全部默认」那一步（地址无哈希）会复原默认，点页内锚点（`#tokens`、`href="#"`）不会把状态清掉 |
| 与进场重播的关系 | 卡片带着自己的地址状态时，构造即记为「已操作」，首次进入视口的进场重播不会把复原的状态冲掉；F11 在截至日不是最新时也记为已操作（它的重播会把截至日改回 9/22）；「重播 / 重播全部」照常恢复默认并清掉对应的键（`replaceState`） |
| 截至日 `AsOf` | 全页只有一个：默认 9/22，范围 9/1–9/22，由 F11「设为截至日」改动（顶部胶囊「回到最新」改回 9/22），写 `asof=09dd`（最新时不写）；变化时通知订阅者，并在 `document` 上派发 `chart-asof` 事件（`detail: {day, key}`）；从带 `asof` 的链接打开时，页面搭建完成后补派发一次 |
| `AsOf` 接口 | `get()`（日序号 1–22）/ `key()`（ISO 日期）/ `today()`（截至日次日，即「今日」，尚无数据）/ `latest()` / `on(fn)` / `set(d)`。`Hash` 与 `AsOf` 只在筛选 / 页签 / 表格模块内可见，不挂 `window`；模块外只能听 `chart-asof` |
| 谁跟随截至日 | 查询层 `q()` 把终点夹到截至日，所以 22 个筛选 / 页签 / 表格组件的读数、月历可选范围（`max` 传函数）、「今日」都自动跟随；指标组件里 K04、K05 监听 `chart-asof`（用 `KpiData.scAt(day)` 现算，从链接打开时同样对齐），其余指标组件仍固定在 9/22 |
| 页面胶囊 | 顶部 `#asof-pill`（`role=status`）写「数据截至 9/22」；截至日被改动时改写为「全页截至 9/14」（按实际日期），变琥珀色并出现「回到最新」按钮 |

### 3.10 `FilterKit`（筛选原语）

| API | 说明 |
|---|---|
| `pop(trigger, panel, {onOpen, onClose})` → `{open(focus), close(back), toggle, place, isOpen}` | 锚定在触发器下方的弹层：`aria-expanded` / `aria-controls` 自动维护；同一时刻只开一个（`FK.current()` 取当前弹层）；点外部、或焦点移到弹层与触发器以外时关闭（不回焦）；Esc 关闭并回焦触发器（焦点还在触发器上时也生效）；所在卡片收到 `metric-hide` 时关闭；**打开后按内容实际高度再定位一次**，下方放不下且上方放得下就翻到上方（`.up`），两边都放不下就把弹层滚进视口；左右越界时平移，窗口 resize 时重新定位；打开期间给所在卡片加 `.fk-raised` |
| `chips(host, {items, mode, value, label, onChange})` | `mode:'single'` = `role=radiogroup` + `role=radio`，方向键移动即选中（单一 Tab 位）；`mode:'multi'` = 切换按钮组（`aria-pressed`），方向键只移动、Space / Enter 切换，`all` 项表示「不设条件」 |
| `toggle(btn, {checked, onChange})` | `role=switch` + `aria-checked`（F09 联动开关） |
| `cal(host, o)` | 月历（W3C APG date picker grid）：单一可聚焦格，← → ↑ ↓ 逐日 / 逐周，PageUp / PageDown 换月（加 Shift 换年），Home / End 到周首尾，Enter / Space 选定；换月时经本卡播报区读出月份标题（如「2026 年 9 月」）；`mode:'range'` 两次点选定起止，悬浮与方向键都预览区间，回调 `onPick({from, to, done})`（第一下 `done:false`），单选回调 `onPick(key)`；`min` / `max`（可传函数，跟随截至日）/ `monthMax`（能翻到的最后一个月）；`isDisabled`、`dayInner`（格内状态点）、`dayLabel`（读屏全文；不给时禁用格自动加「，无数据」）可定制 |
| `multi(anchor, o)` | 可搜索多选：触发器按钮（写「全部 / 华东 / 华东 +2」）+ 弹层（搜索框 `role=combobox` → `role=listbox aria-multiselectable` + 三态「全选」`role=checkbox`）；每勾一项即时生效；重新打开时已选项置顶；触发器上 ↑ / ↓ 打开；搜索框里 ↑ ↓ 移动、PageUp / PageDown 到首尾、Enter 勾选；搜索框为空时 Ctrl / ⌘ + A 全选；底部「清空 / 完成」；Esc 先清搜索再关闭 |
| `applied(host, {items, onRemove, onClear, empty})` | 已选条件 chip（`items` 项为 `{id, label, aria?, dash?}`）：每个 ✕ 是独立按钮（`aria-label="移除 …"`），chip 上 Delete / Backspace 也能移除；末尾「清空全部」调 `onClear`，焦点回本卡 `[data-focus-home]`；**移除后焦点落到原位置的下一个 chip**（删的是最后一个就落到前一个），一个不剩时回到 `[data-focus-home]`；`dash:true` 的 chip 画虚线，表示条件来自图表联动；无条件时显示 `empty` |

### 3.11 `TabKit`（页签原语）

`TK.create(list, {tabs, panels, activation, orientation, ink, selected, hashKey, ids, fromHash, onSelect, onKey})` → `{select(i, {focus, source, silent}), index, tabs, panels, setOrientation, add, remove, refreshInk}`

| 项 | 规则 |
|---|---|
| 结构 | `role=tablist / tab / tabpanel`，`aria-controls` / `aria-labelledby` 互指；面板里没有可聚焦元素时面板 `tabindex=0` |
| 焦点 | roving tabindex：整组一个 Tab 位；横向 ← →、纵向 ↑ ↓（跟随 `aria-orientation`），Home / End 到首尾，首尾循环 |
| 激活 | `auto`：方向键移动即切换（内容已在页面里）；`manual`：方向键只移动焦点，Enter / Space 才切换（切换需要取数，T08） |
| 指示条 | 下划线页签有 2px 指示条（`transform` + `width` 过渡 200ms），纵向时改为右侧竖条；卡内药丸页签不画指示条 |
| 地址 | 给 `hashKey` 时选中项写进地址（`pushState`）：写入值 = `ids(i)`，读回用 `fromHash(v)`（给 `hashKey` 就必须给 `fromHash`）；**首项不写**靠 `ids(0)` 返回 `null`——页面统一用 `tabHash(KEY, () => IDS)` 生成这三项，不给 `ids` 会写数字下标（含 0）；后退 / 前进时按地址切换，键缺失时回到 `selected` |
| 回调 | `onSelect(i, prev, source)`，`source` ∈ `key / click / menu / hash / api`；构造时不回调；由键盘、点击、菜单触发时把页签滚进可视区；`onKey(e, i)` 收到页签组没处理的按键（T08 用它做 Delete 关闭）；`add(tab, panel)` / `remove(i)` 只增删并重连 ARIA，删掉当前页签后不自动选中，由调用方 `select()` |
| 切换动画 | 内容淡入 160ms（`.tk-enter`）；减少动态效果时直接显示 |

### 3.12 `TableKit`（表格原语）

`TBK.create(host, o)` → `{el, host, update(rows, {animate, keepPage}), setAction, setState, setExits, setSort, sort, selected, setFilterOn, rows, lookup, reset}`

| 选项 | 说明 |
|---|---|
| `columns` | `{id, label, rowHeader, num, sum, sortable, type, value, sortValue, compare, render, cls, step, max, label2, color}`；`type` ∈ `amount`（万元 1 位）/ `count` / `yuan` / `rate` / `delta`（涨跌片段）/ `status`（状态胶囊）/ `bar`（数据条 + 百分比）/ `spark`（迷你线，颜色取 `color`）；数值列 `num:true` 右对齐，**表头也右对齐** |
| 必填与回调 | 必填 `key(row)`（行键，排序 / 勾选 / 联动都靠它）与 `caption`（表题，也作滚动区域的名字；`captionVisible` 让它显示）；常用 `rows`、`sort:{by, dir}`、`filterOn`、`emptyText`；回调 `onDrill(row)` / `onSelect(keys)` / `onFilter(keys, key)` / `onSort({by, dir})` / `onRetry()`；`rowLabel(row)` 改写读屏行文 |
| 合计 | `sum:true` 的列在全部行上做**最大余数分摊**（`apportion`），保证显示值逐行相加 = 合计显示值；合计行由 `total(rows, val)` 用显示值生成，不手填 |
| 排序 | 三态：默认 → 降序 → 升序 → 默认；`aria-sort` **只写在当前排序列**上，回到默认时移除；排序按钮在表头内（`.tb-sort`）；行位移 FLIP 420ms；排序与翻页结果经本卡播报区播报 |
| 行点击 `rowAction` | `inspect`（只读，默认）/ `drill`（下钻，行头带 ›，`canDrill` 决定哪些行能钻）/ `select`（勾选，首列复选框 + 表头三态全选；勾选变化经本卡播报区播报「已选 N 行」，全部取消时播报「已取消全部选择」）/ `filter`（联动，行头 ●/○ + `aria-pressed`）；`setAction()` 运行时切换 |
| 键盘 | 行 roving：整张表一个 Tab 位；↑ ↓ 逐行，Home / End，PageUp / PageDown 翻页（仅分页时）；Enter 执行行点击（`drill` / `select` / `filter`），Space 只在 `select` / `filter` 下生效；`onKey(e, row, tr)` 返回真值即接管（B06 的 → ←） |
| 列组 | `groups:[{id, label, cols, tone}]` 生成**真跨列表头**：`th scope=colgroup colspan` + `<colgroup span>`，每个数据格写 `headers="行头 组头 列头"`；`tone` a / b / c 给组底色 |
| 长表 | 超过 `pageAt`（默认 50）行才分页，每页 `pageSize`（默认 20）；分页器写「共 132 行 · 第 1 / 7 页 · 每页 20 行」，当前页码带 `aria-current="page"` |
| 斑马纹 | 默认只用分隔线；当前页可见行数 > 20 且 ≥ 6 列时自动加斑马纹——分页表每页最多 20 行，所以不会触发；`zebra:true / false` 可强制开关 |
| 五种状态 | `setState('ok' / 'loading' / 'empty' / 'error' / 'stale')`：正常 / 加载中（骨架行）/ 暂无数据（`emptyText` 写原因 + `setExits([{label, run}])` 出路按钮）/ 加载失败（重试：先加载 900ms 再回正常，并调 `onRetry`）/ 数据延迟（沿用数据层，表上方加文案固定的「数据延迟」提示条）；四个层叠在同一格子里切换，**等高**不跳，隐藏层 `inert`；`update()` 无行时自动转暂无数据，有行时转回 |
| 横向滚动 | 表宽超出时外框才变成 `role=region` + `tabindex=0` + `aria-labelledby`（可用方向键滚动）；不滚动时不占 Tab 位 |
| 数据条列 | `type:'bar'` 的列宽收缩到内容（`.barcol{width:1%}`），多出来的宽度让给其它列 |
| 读数 | `read(row)` 给读数浮层；`lookup(el)` 交给适配器的 `lookup` |

### 3.13 查询层 `FilterData`（一份事实立方体）

筛选只产生条件，不产生数据；所有读数都是对同一份事实现算。

| 事实（`FACT`，生成时即保证边际相加） | 内容 |
|---|---|
| `regionDayRev` / `regionDayOrd` | 9/1–9/22 × 6 大区的营收（0.1 万元整数）与订单；行和 = 逐日合计（与 KPI `DATA.daily` 一致），列和 = 大区本月 |
| `regionChOrd` | 大区 × 渠道订单（行和 = 大区订单，列和 = 渠道订单）；大区 × 渠道营收取 `REG_CH` |
| `chSegOrd` + `CH_SEG.rev` | 渠道 × 客群（新客 / 复购）的订单与营收；列和 = KPI 的新客 1,012 / 复购 1,406 单 |
| `augRev/augOrd` · `lyRev/lyOrd` | 2026 年 8 月逐日、2025 年 9/1–9/22 逐日；与 KPI 基期合计一致 |
| `GEO` | 15 个省、10 个市、21 个区县（南京、徐州、福州各有一个「鼓楼」）的营收与基期，逐级相加 |

- **客单价各不相同**：大区 华东 5,852 · 华南 5,483 · 华北 5,186 · 西南 4,742 · 华中 4,613 · 其他 5,031 元；渠道 直营 5,891 · 分销 5,292 · 电商 4,595 元；合计仍是 5,320 元（订单按客单价分摊，不按营收比例）。
- **基期结构不同于本期**：8 月的渠道与客群结构按增速反推（电商、新客涨得快，直营、复购慢），每个大区的基期合计不变 —— 所以渠道环比是 +5.1% / +7.9% / +16.0%，而不是全部等于 +8.6%。
- 查询：`q(f, by)` / `qBase(f, by)`，`f = {from, to, regions, channels, segs}`——`from / to` 是 9 月的日序号（默认 1 … 截至日），`regions / channels / segs` 为 id 数组（空 = 不设条件）；`q` 的 `by` ∈ `region / channel / day`，`qBase`（8 月同日）只支持 `region / channel`；不给 `by` 返回 `{rev, ord}`，给了返回 `Map`。`q()` 的终点夹到截至日。`span(fromKey, toKey)` 按 ISO 日期求任意日期段的 `{rev, ord, days, missing, total}`，不受截至日约束；`geo(path, f)` 取省市区，`path` 为点分全路径（如 `east.js.nj.gl`）；`apportion(vals, step)` 最大余数取整（`step` 默认 0.1）。
- 自检：模块加载时用 `check(ok, msg)` 校验八条勾稽（不通过时写 `console.error`，**不抛错**，见 7 · 12）—— 大区 × 日营收（两个方向）与订单、大区 × 渠道营收与订单、客群订单 = KPI 新客 / 复购、8 月与去年逐日 = KPI 基期、省 = 大区、市 / 区县逐级相加。
- 控制台可用 `window.FilterData` 核对任意组合；它另导出 `REG / CH / SEG`、`GEO`、`REG_CH`、`CH_SEG`、`dayVal`、`regById / chById` 等。

---

## 4. 控件规格（按分组）

> 每个控件给出：数据 · 视觉 · 进场 · 交互 · 读数浮层 · 备注。坐标均为 viewBox 单位。文中色值为深色主题；代码里一律写同名 token（如 `#EAC34D` 即 `--s-gold`），浅色值见 2.2。

### 进度与目标

#### 01 活动圆环 Rings ——「入场与超额」从零开始，完整表达每一圈。
- **数据**：MOVE 620/500 KCAL（124%）· EXERCISE 47/60 MIN（78%）· STAND 7/12 HRS（58%）。
- **视觉**：viewBox 460×248；圆心 (116,126)，半径 91 / 69 / 47，环宽 16，圆线帽；轨道 `#2B2D31`；中心 `620` 27px + `KCAL` 9px 字距 2；右侧图例 x=244：色点 + 名称 11px 字距 1.6 + 百分比 31px（取环色的 `-text` 变体）+ 明细 10.5px；三环 token 为 `--s-gold` / `--s-ink` / `--s-sage`（浅色下 EXERCISE 为墨色环）；超额段 `#927127` 从 12 点叠画；徽章 `OVER GOAL`（`#51421A` 底 / `#EDDA93` 字，圆角 11）。
- **进场**：1450ms ease-out，三环、百分比、明细、中心数同步从 0 增长；超过 100% 时超额段与徽章出现。
- **交互**：无手势；悬浮环或图例行显示面板。
- **面板**：`当前 / 目标`、`完成率`、`超额量`。
- **备注**：与视频不同，三环不错峰，同时画出。

#### 03 仪表盘 Gauge ——「指针扫入」一个数字，也有清楚的位置。
- **数据**：示例分数 15 / 64 / 92，默认 64；三段 LOW 0–33 · NORMAL 33–67 · HIGH 67–100。
- **视觉**：viewBox 460×252；圆心 (230,166) 半径 105，弧宽 24，段间各留 1.5°；段色 `#42464B` / `#DE656D` / `#5B5F65`；段名标在弧外（LOW 左、NORMAL 顶、HIGH 右，11px 字距 1.2）；指针 `#F1F0E9` 4px 长 89，枢轴空心圆 r7；读数 34px + 状态 11px 字距 1.8（NORMAL 红、其余 `#C0C4BD` 即 `--c-text-2`）；左下 `FOCUS SCORE`。
- **进场**：从 0 扫到 64，1400ms，回弹缓动（c=0.8）→ 指针略过头再停稳；非当前段弧与标签降到 .7 / .72。
- **交互**：右上「示例」分段控件切换 15 / 64 / 92（850ms，同样回弹）；切换后 svg 的 `aria-label` 改为「专注分数 64，正常区间，满分 100。」；卡内 `.ga-a-status` 被读数层设为 `aria-live=off`，切换本身不主动播报（按钮带 `aria-pressed` 与「示例分数 15，低」式标签）。
- **面板**：`当前 / 满分`、`所属区间`。
- **备注**：视频为 45 / 75 分段与"扫入→过冲→回落"两段式，基准版用单段回弹缓动实现相同观感。

#### 07 目标线 Goal ——「上下拖动」调整标准，立即看见达标情况。
- **数据**：周一至周日 5400 / 7200 / 3800 / 8500 / 6100 / 9100 / 4400 步；默认目标 5000 → 5 / 7 天达标。
- **视觉**：viewBox 480×230；柱宽 28、间距 56，基线 y=194，量程 0–10000（158px）；达标柱 `#E78B5F`，未达标 `#363940`；网格 2500 / 5000 / 7500 `#292C30`（`--c-grid`）；目标线 `#F1F0E9`（`--c-text`）1.6px 虚线 5 5，左端白色胶囊 `GOAL 5.0K`，右端把手 r13 白圆 + 上下箭头；主指标「5 / 7 DAYS」；右上标签「目标可拖动」。
- **进场**：柱从基线升起 850ms（目标线与把手常驻）。
- **交互**：整条线组为 `role=slider`（透明 44px 高热区）；拖动或键盘改目标 → 线、标签、柱色、达标天数实时联动，`aria-valuenow` / `aria-valuetext`（「5000 步，5 天达标」）同步更新；键盘操作时由读数层播报目标线读数，卡内 `.goal-live` 被读数层设为 `aria-live=off`；键盘聚焦时把手显示 `#EAC34D` 3px 描边（指针拖动时不显示矩形焦点框）。
- **面板**：柱 → `步数`、`每日目标`、`超过目标 / 距目标还差`；目标线 → `目标步数`、`达标天数`（拖动中锁定跟随）。

### 时间与趋势

#### 02 打卡热力格 Heatmap ——「点击查看」把连续的日子，铺成一张地图。
- **数据**：固定 20 周 × 7 = 140 天，末日 2026-09-20（UTC）；哈希生成 0–5 次；倒数第 27 天强制为 0（断档），最近 26 天均 ≥1 → STREAK 26 DAYS。
- **视觉**：viewBox 460×236；格 17px、步距 21、圆角 3，起点 (22,29)；五档色 `#2B2D31` → `#565C50` → `#93987B` → `#D4D6BF` → `#74CFB7`（≥4 次同色）；月份首次出现列顶标 `MAY … SEP`；左下 `LESS ■■■■■ MORE`；右下 `STREAK` + `26 DAYS` 25px；右上单位「20 WEEKS · UTC」。
- **进场**：1150ms 线性，按列从左到右由 15% 透明度显现（每列约 0.65×1150/20 ms 错峰），STREAK 数字同步计数。
- **交互**：格为 `role=button`；点击 / Enter / Space 选中（白色描边 2px）；方向键在格间移动（←→ 移一周，↑↓ 移一天），选中态下随焦点更新；Esc 关闭。
- **面板**：`打卡次数`（带档位色点）、`色阶`（4 次及以上 / N 次）。旧版旁弹小卡 `.hm-a-tip` 已由面板层接管（保留代码但隐藏）。
- **备注**：视频里的 `MISSED` 断档标记在基准版中未单独绘制，断档仅表现为深色格。**2026-09-23 决定保持现状**：热力格只表达强度，断档靠深色格与 STREAK 计数说明，不另加标记。

#### 04 柱状图 Bars ——「范围切换 · 单项检查」改变范围，保持比较的参照。
- **数据**：WEEK 7 天 `[34,48,29,63,44,57,47]`（9 月 14–20 日）；MONTH 28 天（9 月 1–28 日）；主指标为范围日均（46 / 47 MIN）。
- **视觉**：viewBox 480×230；基线 y=190，量程 0–80 → 154px；柱色 `#7BAED0` 圆角 3；周柱间距 18、月柱间距 5（宽度自适应 448px）；60 MIN 参考虚线（y=74，`#292C30` 即 `--c-grid`，虚线 2 5，右端标注）；刻度：周 M T W T F S S，月 01 / 07 / 14 / 21 / 28；图下说明行「9 月 14–20 日 · 7 天」。
- **进场**：780ms，柱位置固定、高度与主指标从 0 增长。
- **交互**：WEEK / MONTH 切换 540ms —— 28 根柱的 x、宽、高、透明度与日均一起插值（多出的柱淡入，减少的柱淡出）；点击 / Enter 选中一根：其余退为轨道色，数值由面板给出；再点或 Esc 取消；切换范围清除选择。选中状态写在 `aria-pressed`；键盘聚焦到柱时由读数层播报「周四 · 9/17，专注时长 63 分钟，范围日均 46.0 分钟」，卡内 `.bars-live` 被读数层设为 `aria-live=off`。
- **面板**：`专注时长`、`范围日均`。柱顶胶囊读数（`63 MIN`）保留在代码中、列入 `legacy` 隐藏，数值只由面板给出。
- **备注**：视频为"按住看值 + 先变高再冒新柱"，基准版改为点选 + 单段整体插值（更利于键盘与触屏）。

#### 05 专注分段图 Focus ——「横向拖动」把一天的节奏，读成一条时间线。
- **数据**：09:00–17:00 共 480 分钟，11 个区段 `break 20 · normal 45 · deep 70 · normal 55 · break 25 · normal 50 · deep 60 · normal 65 · break 25 · normal 55 · break 10`；合计 DEEP 2h10m · NORMAL 4h30m · BREAK 1h20m；默认游标 434 分钟（16:14）。
- **视觉**：viewBox 480×220；三行 BREAK y=55 `#85888C` · NORMAL y=104 `#B8C7AB` · DEEP y=153 `#C2D965`，行标 9px 字距 1；区块高 26 圆角 5，相邻区块用 1.1px 竖线 `#4D5748`（`--c-connector`）连接成阶梯；x 轴 09:00 / 11:00 / 13:00 / 15:00 / 17:00；游标：竖线 `#F1F0E9` 72%、当前行处白点 r5 + 12% 光晕 r11、底部 12×4 小把手；顶部读数「16:14」23px + 状态胶囊（底色随状态）；图下汇总行「● DEEP 2h 10m · ● NORMAL 4h 30m · ● BREAK 1h 20m」。
- **进场**：无独立进场动画（重播回到 16:14）。
- **交互**：见 3.6；`role=slider`，`aria-valuenow` 以分钟自 540 起计；`aria-valuetext`「16:14，普通专注」。
- **面板（probe，481 个采样点）**：`状态`（带色点）、`所属时段`、`持续时间`；拖动时锁定跟随。

#### 06 面积图 Area（代码 id `range`）——「连续变形」时间尺度变了，趋势仍然连贯。
- **数据**：1W 7 锚点、1M 9 锚点、1Y 12 锚点（千步），统一重采样为 41 点；主指标为**范围平均值**（1Y 6.5K）；默认 1Y。
- **视觉**：viewBox 480×230；Catmull-Rom 风格三次贝塞尔平滑曲线 `#BAA1D2` 2.6px；面积渐变 0.32 → 0；量程 0–12K 映射 y=190→35（155px），底线与面积底边在 y=193；末端白点 r4.5 深描边；轴标签 4 个（MON WED FRI SUN / 01 SEP 10 20 28 / JAN APR JUL DEC）；图下说明「1–12 月 · 日均步数」。
- **进场**：900ms 从 0 长到目标曲线（面积、末点、指标同步）。
- **交互**：1W / 1M / 1Y 切换 620ms，41 个点原位插值；svg 的 `aria-label` 随之改为「1–12 月 · 日均步数，平均 6.5 千步，末期 9.7 千步；左右方向键选择采样点」；卡内 `.range-live` 被读数层设为 `aria-live=off`，切换本身不主动播报。
- **面板（probe，按锚点数采样）**：`日均步数`、`口径`（当月日均 / 示例采样日）；脚注「读取原始采样值，不读取曲线插值」。
- **备注**：视频为折线 + 末值；基准版改为平滑面积 + 平均值，并把名称由"范围折线图"改为"面积图"。

#### 11 折线图 Line ——「折曲切换 · 点选」同一组数据，两种连线表达。
- **数据**：周一至周日活跃用户 `[32,48,41,65,54,82,72]` 千人；主指标 72（周日）。
- **视觉**：viewBox 480×230；线 `#7BAED0` 3px；数据点 r4（选中 r6 + 白描边 2）；网格 0 / 40 / 80（40、80 虚线）；导引虚线 `#F1F0E9` 40%；说明行「悬停或点选数据点 · 单位：千人」。
- **进场**：1000ms 路径描绘（`pathLength=1` + dashoffset）+ 主指标计数 + 数据点依次淡入。
- **交互**：折线 / 曲线切换 650ms，控制点在"直连"与"平滑"之间插值，数据点不动，当前选中与导引线保留（3.5「切换时清除选择」的例外）；悬停 / 聚焦显示导引线与说明；点击 / Enter 选中；Esc 取消。
- **面板**：`活跃用户`（targets + probe 两种入口）。

#### 12 双柱＋折线组合图 Combo ——「系列开关 · 联动查看」把规模与效率，放在一起阅读。
- **数据**：上半年 / 下半年各 6 个月：营收、成本（万元）；毛利率 = (营收−成本)/营收；主指标为半年合计营收（581 / 740），选中或悬停某月时切换为当月。
- **视觉**：viewBox 480×230；每月两柱（营收 `#B8C7AB`、成本 `#BAA1D2`，宽 14 圆角 3，间距 70）；毛利率曲线 `#EAC34D` 2.5px 贴右轴（0–60%，刻度 0% / 30% / 60%）；左轴 0 / 80 / 160，轴顶标「万元」（轴归属靠单位区分：左「万元」、右 %）；图下图例按钮（营收 / 成本 / 毛利率，可开关，至少保留一个，关闭时划线 + 40% 透明）；说明行「合计：成本 373 万元 · 毛利率 35.8%」。
- **进场**：1100ms 柱高与线描绘同步。
- **交互**：上半年 / 下半年 750ms 插值；悬停（probe，不画导引线）/ 键盘聚焦（仅 focus-visible）/ 点选月份：其他月降到 .28；图例开关；Esc 取消。
- **面板**：`营收`、`成本`、`毛利率`（仅显示已开启系列）。
- **备注**：这是页面中唯一的双轴图（万元 + %）。**2026-09-23 决定保留双轴**，但只在下列条件同时成立时使用：
  1. **两轴单位不同**（金额 vs 比率）；同单位的两个量一律共用一根轴。
  2. **读者要看的是同步走势**（规模与效率是否同向），不是跨轴比大小——线在柱上方不代表任何含义，说明行里也不写这类比较。
  3. **两根轴各自从有意义的起点开始**：柱轴从 0 起；比率轴的量程固定并标注（本例 0–60%），不随数据自动拉伸去放大波动。
  4. **轴与系列一一对应且可辨**：轴标签带单位（本例左轴顶部「万元」、右轴刻度带 %）；最多两组柱 + 一条线，右轴只挂一条线。
  5. **读数浮层给出全部已开启系列的全值与单位（默认三项）**，读者不必对着两根轴估读。
  6. 不满足以上任一条，或读者主要比较绝对量（如经营汇报），改用上下两幅共享横轴的小图，或把两个量指数化到同一基期（=100）后单轴绘制。

#### 13 瀑布图 Waterfall ——「增减拆解」从期初到期末，看清每一步贡献。
- **数据**：实际 `期初 120 · 新客 +48 · 复购 +26 · 人力 −35 · 营销 −19 → 期末 140`；预算 `120 · +60 · +32 · −30 · −16 → 166`；主指标为期末利润。
- **视觉**：viewBox 480×230；柱宽 38 圆角 3，间距 70；总量柱 `#7BAED0`，正向 `#B8C7AB`，负向 `#E78B5F`；相邻柱顶用 `#8A8E86` 虚线连接；柱顶数值（增减带 + 号）；网格 0 / 100 / 200 万元；说明行「期初 120 + 增益 74 − 支出 54 = 140 万元」。
- **进场**：1100ms 柱与数值、连线同步出现。
- **交互**：实际 / 预算切换 750ms，各段连续变化；点选阶段：其他 .35，说明行改为该阶段明细（起始余额 / 正向贡献 / 负向贡献 / 累计结果）；Esc。
- **面板**：总量柱 → `总额`；中间柱 → `本次变动`、`变化前累计`、`变化后累计`。

### 组成与分类

#### 08 分段占比条 Stacked ——「选中上抬」在一个整体里，找到每一部分。
- **数据**：今日 9H 40M（580 分钟）：Focus 38% · Meetings 22% · Learning 16% · Rest 24%。
- **视觉**：viewBox 480×244；条高 48，y=58，总长 422，段间 6px 缝，圆角 6；段色 `#EAC34D` / `#B8C7AB` / `#AAA77C` / `#EFEEE4`；图例 2×2（色点 + 名称 14px + 百分比 15px/650）。
- **进场**：1050ms，各段从左端依次伸展（错峰 0.14），图例随段淡入。
- **交互**：点段或图例 → 该段上抬 14px（300ms），名称与占比由面板给出；其余段保留颜色；再点 / Esc 取消。
- **面板**：`占比`、`时长（按占比折算）`、`总时长 580 分钟`。段上方的读数胶囊（26px 高、1px 引线）保留在代码中、列入 `legacy` 隐藏。

#### 09 环形占比图 Share ——「外移聚焦」从整体组成，走近一个类别。
- **数据**：屏幕时间 5H 12M（312 分钟）：Video 42% · Reading 26% · Games 18% · Music 14%。
- **视觉**：viewBox 480×244；圆心 (131,123) 半径 80 环宽 34，段间各留 1.1°；段色 `#DE88A4` / `#B8C7AB` / `#AAA77C` / `#EFEEE4`，底层轨道 `#2B2D31`（`--c-track`）；中心「5H 12M」24px + `SCREEN TIME` 11px 字距 1.2；右侧图例纵排（色点 + 名称 14.5px + 百分比 15px）。
- **进场**：1200ms，从 12 点顺时针按角度依次画出，图例随各段出现。
- **交互**：点扇段或图例 → 该段沿中角外移 10px（330ms），其余退为 `#3D3E43`、图例变灰，中心换为「26% / Reading」；再点 / Esc 恢复。
- **面板**：`占比`、`时长（按占比折算）`、`总时长 312 分钟`。

#### 10 气泡图 Bubble ——「放大与让位」用大小建立印象，用聚焦展开详情。
- **数据**：Walk 180 · Run 96 · Bike 80 · Yoga 64 · Swim 40 · Gym 24（分钟）；半径 = √分钟 × 4.2。
- **视觉**：viewBox 480×244；比例模式固定坐标 Walk (215,112) `#C2BBF3` · Run (320,78) `#B8C7AB` · Bike (125,177) `#55555C`（浅字）· Yoga (292,185) `#AAA77C` · Swim (126,63) `#EFEEE4` · Gym (365,174) `#EFEEE4`（浅色主题：Bike 仍为深灰气泡，Swim / Gym 为浅灰气泡；圆内文字取各系列 `-on`）；圆内名称 14px/650 + 数值 12px（半径 ≥ 32 时才显示数值）；眉标下的模式行「比例模式 · 圆面积表示时长」。
- **进场**：1000ms，气泡按序从 0 放大（错峰 0.085）。
- **交互**：点击气泡进入**聚焦模式**：被选气泡到 (240,128) r=75 并加描边，模式行改为「聚焦模式 · 圆大小不用于比较」（金色），出现「返回比例」按钮；最小的一个到顶部 (240,24)，其余四个按最近距离分配到四角槽位，半径 ×0.64。运动为弹簧 + 阻尼 + 碰撞的连续积分（240Hz 步长，1500ms），碰撞只加速度不瞬移，半径增大受邻居净空限制。聚焦模式下点另一只气泡直接改聚焦该只并重新分配槽位；被聚焦气泡名称放大到 16px、数值 22px。再点 / Esc / 返回比例 → 回到比例模式（点「返回比例」后焦点回到原气泡）。
- **面板**：`活动时长`、`占总时长`；脚注随模式变化（比例模式：圆面积表示时长 / 聚焦模式：聚焦大小仅用于强调，数值按原数据）。
- **备注**：视频是"被点者放大居中、其余被推开"；基准版改为确定性槽位布局并**明确声明聚焦态的大小不表示数量**，避免误读。

### 结构与策略

#### 14 四象限图（波士顿矩阵）Matrix ——「季度切换 · 单项聚焦」把业务位置，放回增长与份额中。
- **数据**：A 云服务 · B AI 助手 · C 协作套件 · D 旧版工具；每项 [相对市场份额 ×, 市场增长率 %, 营收 万元]；Q1 / Q2 两组；分界 1× / 10%。
- **视觉**：viewBox 480×230；x 轴为对数且**从高到低**（10× 在左、0.1× 在右，`x = 240 − log10(v)·185`），y 轴 0–20%（`y = 194 − v·8.2`）；四象限底色 5.5% 透明（明星绿 / 问题紫 / 现金牛金 / 瘦狗橙）并标名；分界虚线 `#61695C`（`--c-rule`，4 5）；气泡面积 = 营收（r = √营收 × 2.2），按业务固定配色 A 绿 `#B8C7AB`（`--s-sage`）· B 紫 `#BAA1D2`（`--s-purple`）· C 金 `#EAC34D`（`--s-gold`）· D 橙 `#E78B5F`（`--s-orange`），即各项在 Q1 所在象限的颜色，切换季度不变色（Q2 的 B 进入明星象限仍为紫），圆内字母 A–D；轴题「市场增长率」「← 相对市场份额（对数）」；说明行「A 云服务 · B AI 助手 · C 协作套件 · D 旧版工具」。
- **进场**：1000ms 气泡依次放大（错峰 0.12）。
- **交互**：Q1 / Q2 切换 850ms（份额按对数插值，增长率与营收线性），保留当前选中，说明行与面板随之显示新季度的数值（用于追踪单项迁移，3.5「切换时清除选择」的例外）；点选：其余 .3、说明行改为「名称 · 象限 · 份额 / 增长 · 营收」；Esc。
- **面板**：`所属象限`（色点取气泡色，不随象限变）、`相对市场份额`、`市场增长率`、`营收`。

#### 15 树状图 Tree ——「横纵切换 · 分支收起」改变阅读方向，保留层级关系。
- **数据**：总营收 240 → 订阅 120（个人版 45 / 团队版 75）· 服务 80（实施 50 / 咨询 30）· 其他 40（培训 24 / 授权 16），万元。
- **视觉**：viewBox 480×260；节点 64×32 圆角 6，固定槽位：叶层 6 个槽位间隙 12，分支居中于各自两片叶之间（同层等距），层位固定（横向 x = 50 / 240 / 430，纵向 y = 24 / 130 / 236）；根 `#B8C7AB` 深字，分支 `#2B322A`，叶 `#202429`，描边 `#4D5748`；连线三次贝塞尔 `#61695C` 1.2px；分支右上 − / + 符号；说明行「点击分支收起 / 展开 · 同层节点保持等距」；≤700px 隐藏节点数值、名称下移。
- **进场**：1100ms 按深度错峰（根 → 分支 → 叶），连线随之描绘。
- **交互**：横向 / 纵向切换 650ms（前半段淡出、后半段淡入，避免矩形交叉飞行，收起状态与选中都保留）；点分支收起 / 展开子项（350ms 淡出淡入，其它分支槽位不动，`aria-expanded` 同步），同时选中该分支（金黄描边 + 说明行占比；再点只切换收起，不取消选中）；点根 / 叶切换选中（金黄描边）并显示占比；Esc 取消选中。
- **面板**：`营收`、`占父级`、`占总营收`；脚注为路径「总营收 / 订阅」。

### 指标与概览（指标卡 K01–K09 · 指标面板 P01–P07）

> 首批 14 项，按[《指标卡与指标面板方案》](https://claude.ai/artifact/C6rFoUBaTKgn6Z1Qx6FyYZ)的六项决策落地（见 8.6）。方案里的 K07 构成卡、K10 翻牌器、P06 指标树本轮不做，编号保留以便对照。
> 全组共用一份事实数据与一套规则：**事实只写一次，派生值一律现算**；卡面给简写，读数浮层给全值与算法。

#### 4.0 本组共用规则

**一、数据源**（KPI 模块顶部的 `DATA`，一处修改全组同步）

| 字段 | 内容 |
|---|---|
| `asOf` | 截至 9/22：月 22 / 30 天、季 84 / 92 天、年 265 / 365 天；更新于 9/23 09:30（延迟态 01:30） |
| `daily` | 9/1–9/22 逐日营收（万元）、订单、退款单 |
| `hourly` | 9/22 逐小时营收（24 点） |
| `monthly` | 1–9 月营收 / 订单 / 退款单 / 毛利率 / 及时率（9 月只到 22 日，`days[8]=22`） |
| `base` | 四个基期：环比同期 8/1–8/22 · 同比 2025/9/1–9/22 · 上季同期 4/1–6/22 · 去年同期 2025/1/1–9/22 |
| `target` | 月 1,800 万 / 季 4,750 万 / 年 1.56 亿；订单 3,300 单、毛利率 ≥38%、退款率 ≤2%、及时率 ≥98%；**客单价目标 = 营收目标 ÷ 订单目标**（5,455 元，不单独设定） |
| `regions` · `funnel` · `service` · `orderMix` | 区域营收（本月 + 基期各 6 项）、转化链 5 段、服务窗口（本周及时率、三档响应时长）、新客 / 复购 |

**二、派生规则**

- 周期聚合：本月 = 9 月；本季 = 7–9 月；本年 = 1–9 月。合并时**毛利率按营收加权、及时率按订单加权**，客单价 = 营收 ÷ 订单，退款率 = 退款单 ÷ 订单 —— 不做简单平均。
- 目标四件套：达成率 = 实际 ÷ 目标；时间进度 = 已过天数 ÷ 当期天数；差额 = 目标 − 实际；日均需 = 差额 ÷ 剩余天数。
- 预测：近 7 日日均 × 剩余天数 + 当前累计（P02 的预计线与结论句都用它）。
- 自检：模块加载时用 `check(ok, msg)` 校验五条勾稽关系 —— 逐日合计 = 9 月、逐小时合计 = 9/22、区域合计 = 本月与基期、成交 = 订单数 = 新客 + 复购、`scAt(22)` 与 `SC` 一致；不通过时写 `console.error`，不抛错（见 7 · 12）。

**三、数字格式**（`KpiKit.fmt`）

| 情形 | 输入 | 显示 | 规则 |
|---|---|---|---|
| 金额 ≥ 1 万 | 1286.4（万元） | `1,286.4 万元` | 万，保留 1 位 |
| 金额 ≥ 1 亿 | 11466（万元） | `1.15 亿元` | 亿，保留 2 位 |
| 计数 | 2418 / 48210 | `2,418 单` / `4.8 万` | 千分位；≥ 1 万换万 |
| 比率 | 0.384 | `38.4%` | 1 位；浮层可给 2 位（96.44%） |
| 比率之差 | 38.4% vs 37.2% | `+1.2pp` | 写百分点，不写 % |
| 负数 | −0.035 | `−3.5%` | 真减号 U+2212，不用连字符 |
| 空值 / 零值 | `null` / `0` | `—` / `0` | 空值写明原因；零是数据，照常显示 |
| 同一张卡的多个金额 | 1.15 / 1.56 / 0.41 亿 | 同一单位 | 按卡内最大值定单位（`cardScale`）；例外：日均需等每日金额固定用万（41.3 万） |
| 卡面 vs 读数浮层 | 1,286.4 万元 | `12,864,000 元` | 卡面简写，浮层给全值 |

**四、涨跌语义**（`KpiKit.change`：箭头表方向，颜色表好坏）

| 项 | 规则 |
|---|---|
| 箭头 | ↑ 上升 · ↓ 下降 · → 持平，只表方向，对读屏隐藏 |
| 颜色 | 绿 `--st-good-text` = 向好、红 `--st-bad-text` = 向差，由指标的 `polarity` 决定，不看正负 |
| polarity | `up`：营收 / 订单 / 客单价 / 毛利率 / 及时率；`down`：退款率 / 响应时长；`neutral`：只标方向不着色 |
| 单位 | 相对变化写 %，比率之差写 pp |
| 持平 | 四舍五入到 1 位后为 0 记为持平，用灰色 |
| 对比数量 | 每张卡最多两个：主对比（环比同期）+ 次对比（同比） |
| 读屏 | 每张卡的 `aria-label` 读作「客单价 5,320 元，环比（同期）下降 3.5%，向差」 |

**五、判定**

| 类型 | 规则 | 用在 |
|---|---|---|
| 累计型（超前 / 持平 / 滞后） | 达成率 − 时间进度：≥ +0.5pp 超前（绿）· 绝对值 < 0.5pp 持平 · ≤ −0.5pp 滞后（琥珀）。两数都取**卡面显示值**相减，读者可自行核对 | K04 · K05 · P05（P02 不出胶囊，用金额「落后目标节奏」表达） |
| 阈值型 | 三段阈值按**原始值**判档（96.44% 落在 95–98% → 关注），不按四舍五入后的显示值；`polarity:down` 时色段顺序左右翻转 | K06 · P05 |
| 目标型 | 达成 = 实际 ÷ 目标；「减少为好」的指标改为 目标 ÷ 实际（退款率 2.0% ÷ 1.82% = 109.9%）。判档：达成 ≥ 100% 正常 · 95–100% 关注 · < 95% 异常；退款率按实际值判，≤ 上限正常 · ≤ 1.2 × 上限关注 · 否则异常 | P05 |
| 状态胶囊 | ✓ 正常 / ! 关注 / ✕ 异常：图标 + 文字 + 颜色三重编码，底色为 `--st-*-soft` | K06 · P05 |
| 未满周期 | 柱子浅色 + 虚线描边并在读数里注明（K09 的 9 月）；或改用日均（P05 的累计型指标走势） | K09 · P05 |

**六、卡片解剖与尺寸**

```
article.specimen.kpi-spec[.kpi-wide]     ← kpi-wide 跨两列（指标面板）
└─ .specimen-shell                       ← 与图表卡同一张卡：圆角 18、--c-surface、1px --c-border
   └─ .chart-host.kpi-host               ← flex 纵向，高度由内容决定；同一行两张卡等高（shell flex:1）
      ├─ .kpi-bar                        ← 可选：英文眉标 + 周期文字 + 分段控件
      ├─ .kpi-tiles[.one]                ← 1–2 张指标卡；≤700px 单列
      │  └─ .kpi-card                    ← 圆角 18、1px --c-border、padding 15 / 18
      │     ├─ .kpi-head                 ← .kpi-eyebrow（英文眉标 + 中文名）+ ⓘ + .kpi-period
      │     ├─ .kpi-value                ← .kpi-num 28px/600 + .unit + 可选状态胶囊（右对齐）
      │     ├─ 主体                      ← 对比行 / 迷你图 / 进度条 / 进度环 / 阈值刻度 / 排行 / 三联
      │     └─ .kpi-foot                 ← 10px 辅助信息，≤700px 换行
      └─ .metric-dock                    ← ≤700px 的读数停靠区（读数浮层注入）
```

主数值 28px / 600；面板主指标 42px（≤700：34）；三联与宫格 22–24px；页签数值 20px；眉标 10px / 1.5px 字距，中文名 11px / 550；辅助 10–11px；状态胶囊高 22px / 圆角 11；卡内格（P04）圆角 12。

**七、五种状态**（K01 演示，全组通用，同一高度切换不跳动）

| 状态 | 表现 |
|---|---|
| 正常 | 数值 + 单位 + 统计区间 + 更新时间 |
| 加载中 | 骨架条（`--c-track` → `--c-control` 扫光 1.3s）；减少动态效果时不闪 |
| 暂无数据 | `—`（`--c-text-4`）+ 一句原因；读数浮层补充「空值写明原因，零值照常显示 0」 |
| 加载失败 | 一句说明 + 「重试」；重试 → 加载中 900ms → 正常并重播数字 |
| 数据延迟 | 照常显示数值 + 琥珀胶囊「! 数据延迟 8 小时」+ 脚注改为上次更新时间 |

实现：四层放在同一 `display:grid` 单元里用 `visibility` 切换，卡高实测差 <1px；隐藏层的读数目标同步 `aria-hidden`。

#### K01 数值卡 Value ——「状态切换 · 口径悬浮」一个数字，先说清它是什么。
- **数据**：本月营收 1,286.4 万元、订单数 2,418 单（新客 1,012 + 复购 1,406）。
- **视觉**：两张卡并排；眉标 `REVENUE · 本月营收` + ⓘ + 区间 `9/1–9/22`；脚注「更新于 9/23 09:30 / 确认收入口径」（订单数卡右侧为「新客 1,012 · 复购 1,406」）。
- **进场**：数值从 0 计数 700ms（`KpiKit.count`，动画期间锁定终值宽度，单位不滑动）。
- **交互**：分段控件切五种状态；失败态可「重试」。
- **读数浮层**：ⓘ → 区间 / 更新时间 / 单位 + 口径脚注；数值 → 卡面值、完整值 `12,864,000 元`、区间；空值态 → 状态与原因。延迟态下 ⓘ 的更新时间改为 01:30，数值浮层脚注改为「超过 6 小时未更新（最近 9/23 01:30），显示上次结果」。

#### K02 对比卡 Delta ——「悬浮对比项」箭头表方向，颜色表好坏。
- **数据**：客单价 5,320 元（环比 −3.5% 向差、同比 +2.5% 向好）；退款率 1.8%（环比 −0.4pp、同比 −0.3pp，均为向好）。
- **视觉**：两行对比 = 口径名 + 涨跌 + 基期值；脚注左为方向标签「↑ 增加为好 / ↓ 减少为好」，右为单位提示。
- **进场**：两张卡数值从 0 计数 700ms。
- **交互**：悬浮 / 聚焦任一对比行。
- **读数浮层**：本期、基期（比率型 2 位小数，如 1.82% / 2.19%；客单价为整数元）、变化绝对值（−0.37pp / −192 元）、判定；脚注给算式 —— 比率型写「44 ÷ 2,418 = 1.82%；47 ÷ 2,149 = 2.19%。比率之差写百分点。」，相对变化型写「5,320 ÷ 5,512 − 1 = −3.5%。箭头表方向，颜色表好坏。」

#### K03 趋势卡 Sparkline ——「拖过迷你图」这个数，是怎么走到今天的。
- **数据**：本月营收 + 逐日面积迷你图；订单数 + 逐日柱迷你图（各 22 点）。
- **视觉**：迷你图高 46，营收面积图末点加实心点（r3.2），订单柱图无末点；下方首尾标 9/1 / 9/22；读数写在图的**右上方**，默认显示最新一天（灰），探查时变实色。
- **进场**：数值计数 700ms；线从左描出、面积同步淡入（柱图为柱高生长）900ms。
- **交互（D6）**：指针拖过或 ← → / Home / End 逐日（面积图显示空心点与竖虚线，柱图当前柱实色、其余降到 .32）；**主数值始终是本月累计，不跟随**；`role=slider` + `aria-valuetext`；移开或 Esc 回到最新一天。
- **读数浮层**：对比行 → 本期 / 基期 / 变化，脚注重申「读数写在图旁，主数值始终是当期」。

#### K04 目标进度卡 Target ——「周期切换」照这个节奏，能不能完成。
- **数据**：本月 71.5% 滞后 1.8pp · 本季 91.5% 持平 · 本年 73.5% 超前 0.9pp。
- **视觉**：达成率 28px + 状态胶囊（右）；8px 进度条 + **红色虚线时间进度标记**（DNA 签名）+ 标记上方红字「时间进度 73.3%」，标记贴近右端时标签自动改为右对齐；底部四格：目标 / 达成 / 差额 / 剩余天数 · 日均需。
- **进场**：条从 0 画到达成率 800ms，数字同步。
- **交互**：本月 / 本季 / 本年：条长、标记与四格插值 650ms，状态胶囊在切换开始时换为新判定，底色与文字色 .3s 过渡；年视图目标 / 达成 / 差额自动切到「亿」，日均需保持「万」。**跟随页面截至日**：F11 设了截至日后，周期文字变为「本月 9/1–9/14」，达成、时间进度、四格与胶囊按该日现算（`scAt(day)`），同样过渡；带 `asof` 的分享链接打开时，页面启动后即按该日呈现。
- **读数浮层**：进度条 → 达成 / 目标 / 达成率；虚线 → 已过天数 / 当期天数 / 时间进度 + 算法；达成率 → 两数相减得出的超前滞后；差额、日均需各给算式。

#### K05 进度环卡 Ring ——「指标行联动」目标、达成、差距、时间进度，一眼看全。
- **数据**：月业绩 71.5%（蓝）· 年业绩 73.5%（橙）——「月蓝年橙」来自 DNA。
- **视觉**：108×108 进度环（轨道 `--c-track`，弧宽 10，圆头）+ 环心达成率与维度名；环上红色虚线刻度 = 时间进度；右侧四行 = 目标 / 达成 / 差距 / 时间进度，每行带图例块；脚注 = 超前滞后胶囊 + 剩余天数与日均需。
- **进场**：弧从 0 画到达成率 1000ms，环心数字同步，时间刻度在 70% 后淡入。
- **交互**：悬浮 / 聚焦指标行 → 环上对应部分高亮、其余降到 35%（目标 = 轨道变亮、达成 = 弧加粗、差距 = 剩余弧显示虚线、时间进度 = 刻度加粗）；悬浮环本身高亮达成。跟随页面截至日（右上角写「截至 9/14」），两环与四行一起重算；带 `asof` 的分享链接打开时同样按该日呈现。
- **读数浮层**：四行各自的算式；环 → 时间进度与结论。

#### K06 阈值状态卡 Status ——「窗口切换 · 换档」是否正常，图标、文字、颜色一起说。
- **数据**：交付及时率（越高越好，阈值 95 / 98，刻度 90–100%）本周 98.6% 正常 · 本月 96.4% 关注 · 上月 94.8% 异常；平均响应时长（越低越好，阈值 4 / 8 小时，刻度 0–12h）本周 5.4h 关注 · 本月 3.6h 正常 · 上月 8.7h 异常。
- **视觉**：数值 + 状态胶囊（右）；三段色带（各段宽度 = 阈值区间占比）+ 白色指针（外描边 2px 保证在任何色段上可见）+ 四个刻度标签；脚注写三档定义。**越低越好时色段顺序翻转**（左绿右红），颜色语义不变；卡头周期随窗口写「本周 9/16–9/22 / 本月 9/1–9/22 / 上月 8/1–8/31」。
- **进场**：默认本月；指针与数值从刻度左端（90% / 0 小时）滑入本月值 800ms。
- **交互**：本周 / 本月 / 上月，指针滑动 600ms、数值插值；胶囊在同一元素上换档，底色与文字色 .3s 过渡。
- **读数浮层**：数值 → 原始值 / 显示值 / 判档（脚注：按原始值判档，不按显示值）；色带 → 三档阈值定义。

#### K08 排行卡 Top-N ——「排序切换 · 行位移」谁贡献最多，谁涨得最快。
- **数据**：华东 412.6 / 华南 298.3 / 华北 236.5 / 西南 171.2 / 华中 102.4 / 其他 65.4（万元，合计 = 本月营收）；环比同期增速 +7.4% / +13.3% / +3.5% / +19.3% / −2.8% / +9.0%。
- **视觉**：六行 = 名次 + 名称 + 条 + 数值，行距 25；「其他」名次为「—」、条与数值弱化，**不参与排名**；增速视图从 0% 竖线向两侧发散，负值条用石板灰，数值用好坏色。
- **进场**：条从 0 生长 900ms。
- **交互**：营收 / 增速切换 → 各行移到新名次（`translateY` 插值 600ms），条长与数值同步变化，DOM 顺序同时重排以保证读屏与 Tab 顺序一致。
- **读数浮层**：营收 / 占比 / 环比 / 两种口径下的名次 + 基期值。

#### K09 日 / 月 / 年三联卡 Trio ——「分尺度读数」同一个指标，三种时间尺度。
- **数据**：日 9/22 66.4 万（较上周同日 +5.6%，避开周末效应）· 月 1,286.4 万（环比同期 +8.6%）· 年 1.15 亿（同比 +17.9%）。
- **视觉**：一卡三列、细竖线分隔；每列各自的粒度迷你柱 —— 日按小时（24）鼠尾草绿、月按日（22）雾蓝、年按月（9）橙；**9 月未满月**：柱子 45% 不透明度 + 虚线描边，读数注明「9 月（1–22 日）」；年列单位自动为「亿元」。
- **进场**：三个数值从 0 计数 700ms，迷你柱从 0 生长 900ms。
- **交互**：三条迷你图各自可拖 / 可键盘探查，读数写在图旁；主数值不跟随。
- **读数浮层**：各列对比口径 → 本期 / 基期 / 变化 + 口径说明。

#### P01 指标条 KPI strip ——「口径切换 · 方向键」一行读完核心指标。
- **数据**：营收 / 订单数 / 客单价 / 毛利率 / 退款率 / 交付及时率（本月）。
- **视觉**：一卡六列、细竖线分隔；≤960px 三列两行，≤700px 两列三行（换行处补横线）。
- **进场**：六个数值同时计数 700ms，涨跌从 0 插值。
- **交互**：面板级「环比 / 同比」切换，六项涨跌同步插值 500ms、颜色随插值中的数值实时改变（毛利率由 +1.2pp 变到 −0.6pp 时绿转红）；组内方向键移动（roving tabindex，整组只占一个 Tab 位），Home / End 到首尾。
- **读数浮层**：本期 / 基期 / 当前口径变化 / 另一口径变化 + 指标口径。

#### P02 主次面板 Hero + list ——「指标行解释图表」主指标怎么样，还差多少。
- **数据**：主指标本月营收 1,286.4 万（42px）；右列 达成率 71.5% · 时间进度 73.3% · 落后目标节奏 33.6 万 · 剩余 8 天日均需 64.2 万 · 近 7 日日均 64.5 万；结论：按近 7 日节奏月底预计 1,802.5 万，可完成但只多 2.5 万。
- **视觉**：左为主指标（眉标带 ⓘ，下方两行对比：环比（同期）+8.6%、同比 +21.3%）+ 累计曲线（蓝线 + 淡面积）、红色虚线目标节奏、灰虚线月目标、今日竖线、蓝点线预计；右为指标行 + 结论框（`--c-inset`）。≤960px 上下排列。
- **进场**：主数值计数 800ms；累计曲线按天逐段画出 1100ms，预计线在结束后出现。
- **交互**：悬浮 / 聚焦右侧指标行 → 图中对应元素高亮（达成率 = 加粗曲线、时间进度 = 今日竖线、落后节奏 = 今日处的红色差额段与数值、日均需 = 从今天到目标的点线、近 7 日日均 = 预计线加粗），其余降到 20–35%；曲线本身用 probe 逐日探查。
- **读数浮层**：逐日 → 累计营收 / 目标节奏 / 较节奏 + 当日值；各指标行 → 算式；ⓘ → 统计区间 / 更新时间 / 口径；两行对比 → 本期 / 基期 / 变化；主数值不随探查改变。

#### P03 指标页签驱动图表 Tabs → chart ——「页签切换 · 原位变形」从一排指标，走进一张图。
- **数据**：四个页签（营收 / 订单数 / 客单价 / 退款率）各自的逐日序列与日目标：60.0 万 / 110 单 / 5,455 元 / 上限 2.00%。
- **视觉**：页签为卡片式按钮，选中 = `--c-inset` 底 + 顶部 2px 蓝色指示条；下方折线 + 逐日圆点 r2.4 + 淡面积 + 目标虚线 + 左侧刻度；坐标域按每个指标取整（`niceDomain`）；≤960px 页签 2×2。
- **进场**：曲线从底线升起 1000ms，圆点与目标线同步淡入。
- **交互**：点击或 ← → / Home / End 切换（`role=tablist`，roving tabindex）；切换时曲线在同一坐标系内**原位变形** 650ms（归一化位置插值），目标线随之滑到新位置，刻度标签与目标标签在中点淡出淡入，网格线在中点换成新刻度；probe 逐日探查（当前点换为 r4.5 空心点 + 竖虚线）；**页签上的数值始终是本月，不随悬浮改变**。
- **读数浮层**：当日值 / 日目标 / 较目标（带好坏判定）。

#### P04 宫格看板 Grid ——「面板级周期切换」同一时间尺度下的一组指标。
- **数据**：六格 = 指标条同一组指标；周期 本月 / 本季 / 本年，对比口径随之变为 环比同期 / 上季同期 / 同比。
- **视觉**：3×2 等尺寸格（`--c-divider` 描边、圆角 12）；≤700px 2×3。
- **进场**：六格数值从 0 计数、涨跌从 0 插值 700ms。
- **交互**：面板级周期切换，六格数值与涨跌同时插值 600ms；金额跨量级时按目标周期的单位显示（本年营收 1.15 亿元、订单 2.2 万单）。
- **读数浮层**：本期 / 基期 / 变化 + 合并口径说明（毛利率按营收加权、及时率按订单加权、客单价与退款率逐期重算）。

#### P05 记分卡表格 Scorecard ——「列排序 · 行读数」所有指标对目标的全貌。
- **数据**：六行 × 七列 —— 指标 / 本月 / 目标 / 达成 / 同比 / 1–9 月走势 / 状态。关注三项：营收（滞后 1.8pp）、客单价（达成 97.5%）、交付及时率（96.4% 落在 95–98%）。状态判定：营收 / 订单数按累计型（滞后 → 关注，超前 / 持平 → 正常，不判异常）；客单价 / 毛利率 / 退款率按目标型；交付及时率按阈值型 95 / 98（见 4.0 · 五）。
- **视觉**：行头列 `position: sticky` 便于窄屏横向滚动；数值右对齐等宽（表头同样右对齐）；走势为迷你线；状态为胶囊。**累计型指标的走势用日均**（营收、订单数），避免 9 月未满月造成假下跌。已改用 `TableKit`（3.12），外观不变。
- **交互**：点击「达成」或「状态」表头**三态排序**：降序 → 升序 → 默认（表格组决策 D2）；`aria-sort` 只写在当前排序列，回到默认时移除；「状态」降序 = 最严重在前（异常 › 关注 › 正常），同档按达成从低到高；排序用 FLIP 位移 420ms，减少动态效果时直接重排；表体一个 Tab 位（「达成」「状态」两个排序按钮另各占一个），↑ ↓ / Home / End 在行间移动并显示读数。
- **读数浮层**：本月 / 目标 / 达成 / 同比 / 状态 + 判定依据（如「达成 71.5% vs 时间进度 73.3% → 滞后 1.8pp」）与走势口径。

#### P07 转化链 Chip chain ——「转化口径切换」每一步，留下了多少。
- **数据**：访客 48,210 → 注册 12,684 → 线索 6,532 → 商机 3,988 → 成交 2,418（= 订单数）。
- **视觉**：五枚等宽胶囊，底色按阶段顺序由浓到淡（`color-mix` 100 / 78 / 58 / 40 / 24%，前两级用 `--s-blue-on` 深字，后三级用 `--c-text`，两套主题各自达标）；胶囊下为数值与转化率；脚注「整体转化 5.0%（2,418 ÷ 48,210） · 成交 2,418 = 订单数」与「色越浓，阶段越靠前」；≤700px 改为竖向，箭头旋转 90°。
- **进场**：人数从 0 计数、转化率从 0 插值，均 700ms。
- **交互**：逐步 / 累计口径切换，转化率插值 500ms。
- **读数浮层**：人数 / 上一步 / 逐步转化 / 累计转化 / 本步流失（全值，不用万）。

### 筛选与查询（F01–F11）

> 按[《筛选器组件方案》](https://claude.ai/artifact/LoifuCkEE1jVvzxHbE8Jnc)的 9 项决策落地（「十一项一次做完」）。原语见 3.10，页面级状态见 3.9，数据见 3.13。
> 一句话原则：**筛选只产生条件，读数都从同一份事实现算**；每张卡都写清当前条件、结果和口径。

#### 4.F 本组共用规则

| 项 | 规则（决策编号见方案） |
|---|---|
| 生效方式（D2） | 单个控件**即时生效**；抽屉里的多项改动**批量应用**（「未应用」计数 + 应用 / 重置 / Esc 放弃） |
| 时间上下文（D3） | 全页只定**截至日**（F11 设定，顶部胶囊显示）；「本月 / 近 7 天 / 对比期」这类口径由每张卡自己定 |
| 单击图表（D4 · D9） | 单击 = 选中 + 联动其余图表；看板级「联动」开关默认开，关掉后单击只高亮；联动条件以**虚线 chip** 回显，与主动筛选取交集，来源图不被自己过滤 |
| 级联（D5） | 按**整条路径**匹配：显示值（鼓楼）与条件值（江苏 / 南京 / 鼓楼）分开存 |
| 日期面板（D6） | 状态点按**星期加权**的日目标判定：9 月工作日 64.3 万、周末 48.2 万（周末权重 0.75，月目标 1,800 万）；达标 ≥100% · 接近 90–100% · 未达 <90% |
| 地址（D7） | 条件写进 `#!` 哈希，可分享、可后退（3.9）；按 `URLSearchParams` 序列化，`, ; = ~` 在地址栏里写成 `%2C %3B %3D %7E`，下文示例均为解码后的值（未编码的写法同样能读回） |
| 外观（D8） | 输入类（触发器、搜索框）**方角** 8px；选项类（chip、快捷项）**药丸** |
| 选中语法 | 与分段控件一致：反色底（`--c-inverse`）+ `aria-pressed / aria-checked`；平铺多选 chip（F06、F08 大区）同样用反色 + `aria-pressed`；下拉 / 级联列表里的多选用复选框 + ✓（F04、F05），不靠色块 |
| 空值 | 没数据写原因（「今日数据尚未产出」「对比期无逐日数据」），不写 0；空结果给原因 + 可点的出路 |
| 读数 | 结果行或主数字都能悬浮 / 聚焦看读数（F08 为结果表的行）；F09 条形可聚焦，F07 逐步条、F10 日格、F11 周条只能悬浮；F01 / F02 / F09 的逐日柱、F03–F05 的条形与 F06 矩阵只作示意、不出读数；读数写全值与口径 |

#### F01 快捷时间 Time chips ——「单选 · 方向键」先把时间定下来。
- **数据**：今日 / 昨日 / 本周 / 本月 / 本季 / 本年 / 近 7 天 / 近 30 天 / 自定义；默认本月 = 9/1–9/23 · 23 天（有数据 22 天）1,286.4 万元，较上月同期 +8.6%。「今日」= 截至日的次日（9/23），没有数据。
- **视觉**：一行药丸 chip（`role=radiogroup`），选中反色；下方结果卡 = 区间 + 主数值 + 对比 + 订单 / 日均 / 客单价 + 逐日柱（无数据的日子画虚线空位）。
- **交互**：点击或方向键移动即选中；「自定义 ▾」打开月历弹层（区间模式，确定 / 取消），关闭后焦点回到触发器；「今日」显示空态与原因。
- **对比口径**：日 / 周比上周同期，月比上月同期，本季比上季同期、本年比去年同期（用指标模块的固定基期，只在截至日为 9/22 时有值，否则写「—」并说明），滚动区间与自定义比前一段等长区间 —— 逐日口径**只在有数据的日子上比**，不拿 22 天比 23 天。
- **地址**：`f01=d7`，自定义 `f01=c0919-0920`；默认本月不写。

#### F02 日期区间 Date range ——「预设 + 月历」这段时间到底是哪几天。
- **数据**：默认 9/1–9/22 对比 8/1–8/22：营收 1,286.4 万 +8.6%、订单 2,418 单 +12.5%。
- **视觉**：触发器「时间 9/1 – 9/22 ▾」+ 对比 chip；弹层三段 = 左侧预设（近 7 天 / 近 14 天 / 本月 / 上月 / 8/1 至今）、右侧区间月历、底部对比期（上期同期 / 去年同期 / 不对比）+ 「已选 22 天 · 有数据 22 天 · 时间进度 73.3%」+ 取消 / 确定；结果区为本期 / 对比期成对柱。
- **交互**：预设与月历二选一，改动先在弹层里预览，确定后本期与对比期一起更新；对比期跟着区间走（上期同期 = 每一天往前挪一个月）。
- **空值**：对比期落在没有逐日数据的月份（7 月、2025 年 8 月）时，读数写「vs 上期同期：— 无逐日数据」、卡头写「对比期无逐日数据」；只缺一部分时写「— 缺 N 天」「对比期缺 N 天数据」；不补 0。
- **地址**：`f02=0901-0922.yoy`（`mom / yoy / none`）。

#### F03 单选下拉 Single select ——「方向键 · Enter 确认」在几个互斥口径里选一个。
- **数据**：业务线 全部 / 直营 556.1 / 分销 402.7 / 电商 327.6（万元）；选中后给占比、订单、客单价、环比同期。
- **视觉**：方角触发器（`role=combobox`，select-only）；列表项右侧写数值，选中项 ✓；下方条形图里未选中的业务线降为浅色，保留比较参照。
- **交互**：↓ / ↑ / Enter / Space / Home / End 打开；方向键只移动高亮（`aria-activedescendant`），**Enter、Space 或 Tab 才确认**，Alt + ↑ 确认并关闭；首字母跳转（输入「分」到分销）；Esc 关闭不改值。
- **地址**：`f03=direct`。

#### F04 多选下拉 Multi select ——「搜索 · 计数 · 全选」同时看几个，且知道选了几个。
- **数据**：默认华东 / 华南 / 华北 → 947.4 万元，占全部 73.6%，订单 1,705 单，环比同期 +8.2%。
- **视觉**：触发器写「区域 华东 +2」+ 右侧「已选 3 / 6」；弹层 = 搜索框 + 三态「全选」+ 列表（复选框 + 名称 + 数值）+ 底部计数与清空 / 完成；下方条形图未选中项降为浅色。
- **交互**：每勾一项**即时生效**；搜索后 Enter 勾选第一项；Ctrl / ⌘ + A 全选；Esc 先清搜索、再关闭；**重新打开时已选项置顶**。
- **备注**：候选超过 100 时先搜后选（卡头的规则说明；本例 6 个候选）。
- **地址**：`f04=east,central`；默认不写，全不选写 `none`。

#### F05 级联筛选 Cascader ——「逐级展开 · 全路径」沿着地理往下收。
- **数据**：默认 鼓楼（南京）+ 鼓楼（福州）→ 45.7 万元，占全部 3.6%；省市区树见 3.13。
- **视觉**：触发器写「区县 鼓楼（南京）+1」；弹层三列 省 / 市 / 区，列里写已选数；已选路径逐个成 chip（「江苏 / 南京 / 鼓楼 ✕」）；结果条形图的标签写到能区分为止（「鼓楼（南京）」）；下方一句重名说明。
- **交互**：→ 进入下一级、← 返回上一级，Space 勾选区县；只在末级勾选；清空全部。
- **地址**：`f05=east.js.xz.gl`（整条路径，多项用逗号分隔；默认两项不写，全部清空写 `none`）。

#### F06 维度 chips Facet chips ——「平铺多选」候选很少时，别用下拉。
- **数据**：渠道 × 客群 2×3 矩阵，每格写营收与订单（如 直营 × 复购 358.1 万 / 583 单）。
- **视觉**：一维一行，行首写维度名；「全部」= 这一维不设条件；下方矩阵里**命中的格子反色**，其余降为浅色；底部写营收 / 订单 / 客单价。
- **交互**：同维取「或」、两维取「且」；方向键在行内移动，Space 切换。
- **备注**：图例不承担筛选 —— chips 只放在数据上方。
- **地址**：`f06=direct~repeat`（默认 `all~repeat` 不写）。

#### F07 已选条件回显 Applied chips ——「逐个移除」我现在到底在看哪一段数据。
- **数据**：默认四个条件（时间 近 7 天 · 区域 华东 +2 · 渠道 电商 · 客群 复购〔来自图表联动〕）→ 44.8 万元 · 94 单 · 占全部 3.5%。
- **视觉**：chip 行 + 「清空全部」+「＋ 添加条件」；虚线 chip = 来自图表联动；下方「每个条件收窄了多少」按回显顺序逐步叠加（全部 → 近 7 天保留 35.1% → … → 复购保留 55.2%）。
- **交互**：✕ 或 Delete / Backspace 移除，**焦点落到下一个 chip**；「添加条件」弹层列出被移除的条件；全部都在时按钮禁用，原因「示例条件都已加上」写在悬浮提示（`title`）与 `aria-label` 里。
- **地址**：`f07=time,region`（剩下的条件；全在不写，全空写 `none`）。

#### F08 筛选抽屉 Filter panel ——「批量应用」筛选项太多时收起来。
- **数据**：默认 大区 华东 / 华南 / 华北 · 营收 50–210 万 · 环比 增长 → 符合 7 / 15 个省份 · 901.0 万元 · 占全部 70.0%；结果表用 `TableKit`（可排序、合计由显示值相加）。
- **视觉**：「筛选 · 3」按钮 + 已应用条件 chip；抽屉从卡片右侧滑出（`role=dialog aria-modal`，宽 360，≤700px 占满卡宽，高度不超过视口），内含大区 chips、营收区间双滑块、环比 chips；底部「N 项未应用」+ 重置 / 应用。
- **交互**：抽屉内改动先记为「未应用」，**「应用」一次提交**；✕ / Esc / 点遮罩 放弃改动并回焦「筛选」按钮；焦点锁在抽屉内；打开时整只抽屉滚进视口。空结果时表格给出路按钮：营收区间放宽到全部（区间收窄时）/ 移除「大区」（选了大区时）/ 清空全部。
- **地址**：`f08=r=south;v=0-210;g=all`（打开时的示例状态「华东 / 华南 / 华北 · 50–210 · 增长」不写；清空后写 `r=;v=0-210;g=all`）。

#### F09 图表联动 Cross-filter ——「单击即联动」点了图，别让人猜发生了什么。
- **数据**：区域营收（6）、渠道构成（3）、逐日营收（当前条件 vs 全部成对柱）；范围 本月 / 近 14 天 / 近 7 天 + 区域多选。
- **视觉**：右上角「联动 开 / 关」开关（`role=switch`）；三图并排（≤960 两列、≤700 单列）；被点项用金黄高亮、其余降为浅色；联动条件以虚线 chip 出现在条件行；图下一行灰字写当前模式。
- **交互**：单击条形 = 选中 + 联动其余两图；再点同一项、点 chip 的 ✕ 或 Esc 撤销；两个来源的条件取交集（区域 华东 ∩ 渠道 电商）；**来源图不被自己的条件过滤**；关掉开关即清除联动，之后单击只高亮；区域多选把联动的区域筛掉时联动条件自动清除并播报。
- **读数浮层**：条形 → 营收 / 订单 / 占本图 + 「本图不受自己的联动影响」；结果行 → 时间 / 筛选区域 / 联动 / 营收。
- **地址**：`f09=t=d7;g=east,south;lr=east;lc=online`；关掉联动只写 `x=0`（同时清除 `lr / lc`）。

#### F10 结果条与空态 Result & empty ——「阈值拖动」筛完之后，还能往哪走。
- **数据**：区域 chips + 单日营收阈值（0–80 万，步长 5）；默认 ≥ 60 万 → 符合 10 / 22 天 · 合计 668.7 万元 · 占区间 52.0%。
- **视觉**：结果条常驻；22 格日条（符合 = 蓝、不符合 = 轨道色、截至日之后 = 空心格（1px 细框））；空结果时整块换成「原因 + 出路按钮」。
- **交互**：拖动阈值或换区域即时生效；空结果的出路直接改写条件：阈值放宽到能命中的值 / 移除「区域」（选了区域且移除后能命中时才出现）/ 清空全部。
- **地址**：`f10=central~40`。

#### F11 日期面板 Date panel ——「按日巡检 · 设截至日」哪天没达标，点进去看。
- **数据**：9 月逐日营收对星期加权日目标；9/1–9/22 达标 8 · 接近 9 · 未达 5。
- **视觉**：三栏（≤1100 两栏、≤700 单栏）= 月历（格下状态点：绿 / 琥珀 / 红 / 空心 = 无数据）+ 所选日读数（营收 / 日目标 / 达成 / 订单 / 客单价 / 较上周同日 + 本周小结）+ 月度巡检（按周的达标 · 接近 · 未达堆叠条）。
- **交互**：月历按 APG 日期网格（3.10）；截至日之后的日子不可选；「设为截至日」把全页截至日改为所选日（顶部胶囊变色，筛选、页签、表格与 K04、K05 按该日重算），再点「回到最新」恢复。
- **地址**：`f11=0914`（所选日）；截至日另写 `asof=0914`。

### 切换与导航（T01 · T02 · T05–T08）

> 按[《Tab 组件方案》](https://claude.ai/artifact/EZPkHwQ8a7MqXJxcx5qC7o)的 6 项决策落地（「六种一次做完」）。编号沿用方案：T03 指标页签由 P03 承担，T04 分段控件沿用模板现有按钮组，第一版的 T09 两层结构改为「最多两层」规则 —— 所以只有 T01 T02 T05–T08 六张卡。原语见 3.11。

#### 4.T 本组共用规则

| 项 | 规则 |
|---|---|
| 样式（D3） | **页面级用下划线**（最轻，选中 = 加粗 + 2px 指示条）；**卡内用药丸**（选中反色） |
| 切什么（D4） | 切**内容**用页签，切**参数**（营收 / 订单）用分段控件 `.segmented`（按钮组语义，`aria-pressed`），两层语义不同，不混用 |
| 激活（D2） | 内容已在页面里 → 自动激活（方向键即切换）；切换要取数 → 手动激活（方向键只移焦点，Enter / Space 才切换） |
| 层级（D5） | 最多两层：页签里可以再放分段控件，不再嵌第三层 |
| 放不下 | 先横向滚动 + 边缘渐隐 + 两端箭头；数量不可控时再加「更多 ▾」菜单 |
| 高度 | 同一组面板共用最小高度，切换时页面不跳（T01 284px、T02 258px、T05 列表区固定 262px 内部滚动、T08 桌面 236px） |
| 地址（D6） | 选中页签写进地址，首项不写：`t01=trend`、`t02=region`、`t05=watch`、`t06=east.js`、`t07=channel`、`t08=online` |

#### T01 下划线页签 Line tabs ——「自动激活」页面这一层，我在看哪一块。
- **面板**：总览 = 四块数（营收 / 订单 / 客单价 / 日均营收，各带逐日走势线）+ 目标进度条（达成 71.5%、时间进度红虚线 73.3%、滞后 1.8pp）+ 三条要点（涨得最快 / 唯一下滑的大区 / 客单价与订单的关系，均由数据现算）；趋势 = 逐日营收柱 + 7 日均线（按真实像素宽重绘，截至日之后留空位，probe 逐日探查）；结构 = 区域营收一条、渠道与客群各两条（营收 / 订单）堆叠条，两条的错位就是客单价的差；明细 = 区域表（`TableKit`，含客单价列）。
- **交互**：← → 切换并立即显示；Home / End；Tab 进入面板；刷新或复制链接回到同一页签。
- **读数浮层**：数字块 → 本期 / 同期 / 环比 + 口径；色段 → 营收 / 订单 / 两种占比 / 客单价；趋势逐日；表格行。

#### T02 药丸页签 Pill tabs ——「卡内切换 + 参数」卡片内部换一组内容。
- **面板**：渠道 / 区域 / 客群，各为「合计 + 个数 + 环比」一行 + 条形（数值 + 环比同期）+ 一条结构条（同一指标的占比）；右上角分段控件切营收 / 订单。
- **数据**：渠道环比 直营 +5.1% · 分销 +7.9% · 电商 +16.0%；客群 新客 +15.9% · 复购 +4.3%（基期结构不同于本期，见 3.13）。

#### T05 徽标页签 Badge tabs ——「处理 · 计数」哪一组还有事没处理。
- **数据**：待办 = 截至日及之前未达日目标的日子（默认 5 条）；关注 3 条；已处理 2 条；全部 10 条。
- **视觉**：徽标只放数量，为 0 时隐藏，「全部」页签不显示徽标；关注用琥珀徽标；每条 = 状态胶囊 + 两行说明 + 操作按钮（标记已处理 / 移出关注 / 撤回；「全部」页签只列不操作）；列表区固定高度 262px，超出时内部滚动，底部渐隐提示还有内容。
- **交互**：处理后徽标实时减少，焦点落到同组下一条的按钮（处理的是最后一条就落到新的最后一条，列表空了回到页签）；数量写进页签 `aria-label`（「待办，4 项」），徽标本身 `aria-hidden`；截至日变化时待办重算。

#### T06 页签溢出 Overflow ——「滚动 · 更多」页签放不下的时候。
- **数据**：15 个省份按营收排序；面板 = 营收 / 占大区 / 大区内排名 + 下级城市条形（按营收排序；只有沪苏浙粤闽 5 省有城市数据，其余 10 省写明没有下级城市数据）。
- **视觉**：横向滚动条隐藏，两端渐隐 + ‹ › 箭头（到头时禁用）；右侧「更多 ▾」菜单按大区分组列出全部省份（`menuitemradio`，✓ 标当前）。
- **交互**：方向键移动时自动把目标页签滚进可视区；菜单支持方向键、Home / End、首字跳转、Esc；菜单在下方放不下时翻到按钮上方；「更多」是菜单按钮，不是页签。

#### T07 纵向页签 Vertical ——「↑ ↓ 切换」左边一列，右边一页。
- **面板**：经营总览（三块数 + 逐日营收小柱）· 渠道（营收与环比 + 营收 / 订单结构条 + 客单价说明）· 区域（营收与环比）· 客群（两块数 + 各渠道复购占营收）· 履约（三块数 + 1–9 月及时率走势，虚线 = 98% 目标；按周更新，不随截至日变化）。
- **视觉**：左列 128px，选中 = `--c-inset` 底 + 右侧 2px 竖条；**≤700px 自动变成横向滚动**（下划线样式），`aria-orientation` 同步切换，方向键随之改为 ← →。
- **备注**：条目多、文案长的设置型页面用；不作主导航（Carbon）。

#### T08 动态页签 Dynamic ——「手动激活 · 新建关闭」页签由用户自己加。
- **数据**：我的视图（固定）/ 华东复盘（区域 = 华东）/ 电商专题（渠道 = 电商）；每个视图 = 条件说明 + 三块数（带走势线）+ 分解条形。
- **交互**：方向键只移动焦点，**Enter / Space 才切换**，切换时骨架屏 380ms（`aria-busy`）；「＋ 新建视图」最多 6 个；Delete 关闭当前页签；页签上的 ✕ 只给鼠标（`aria-hidden`、不进 Tab 顺序），键盘与读屏用面板里的「关闭此视图」；关闭后焦点落到相邻页签。
- **焦点环**：页签在横向滚动容器里，焦点环画在页签内侧（`outline-offset:-3px`），不会被容器裁掉。

### 表格与明细（B01 · B03 · B05–B07）

> 按[《表格组件方案》](https://claude.ai/artifact/WoQdwRtxaxVRVLiJc4JboE)的 7 项决策落地（「五种一次做完」）。编号沿用方案：B02 记分卡表 = P05（已改用 `TableKit`，外观不变），B08 排行表 = K08，B04 单元格视觉、B09 排序与表头、B10 四态改为下面的共用规则 —— 所以只有 B01 B03 B05–B07 五张卡。原语见 3.12。

#### 4.B 本组共用规则

| 项 | 规则 |
|---|---|
| 排序（D2） | **三态**：默认 → 降序 → 升序 → 默认；`aria-sort` 只写在当前排序列；排序按钮写「已按营收降序排列」播报 |
| 分隔（D3） | 默认只用行分隔线；超过 20 行且 ≥ 6 列才加斑马纹 |
| 条件格式（D4） | **数据条**（占比、组内占比）+ **状态胶囊**（图标 + 文字 + 颜色）；不用整格底色 |
| 行点击（D5） | **按表的用途配置**：只读 / 下钻 / 勾选 / 联动，由 `rowAction` 指定，B01 演示四种 |
| 列组表头（D6） | **真跨列**：`colspan` + `scope=colgroup` + `<colgroup span>` + 数据格 `headers` |
| 长表（D7） | ≤ 50 行整表显示，超出后每页 20 行分页 |
| 对齐 | 文字左对齐，数值右对齐且等宽数字，**数值列表头也右对齐** |
| 合计 | 合计行由显示值相加（最大余数分摊），读者自己加得出来 |
| 五种状态 | 正常 / 加载中 / 暂无数据 / 加载失败 / 数据延迟，同高叠放 |

#### B01 明细表 Detail table ——「行点击可配置」具体是哪一行。
- **数据**：区域粒度 6 行（营收 / 订单 / 客单价 / 环比同期 / 占比）；切到「区域 × 日」= 132 行，自动分页（每页 20）。
- **交互**：右上角两组分段控件 —— 粒度（区域 / 区域 × 日）与行点击（只读 / 下钻 / 勾选 / 联动）：
  - 只读：悬浮或 ↑ ↓ 聚焦看读数；
  - 下钻：仅区域粒度（切到「区域 × 日」时回到只读）；有 › 的行可下钻，点行进入该区域的省份，上方面包屑变为「全部区域 › 华东」，点「全部区域」返回；
  - 勾选：首列复选框 + 表头三态全选，上方写「已选 N 行 · 合计 … 万元 · … 单 · 清除」；
  - 联动：点行把该区域作为条件（虚线 chip「区域 = 华东 · 来自 明细表」，可多选），表格下方的渠道构成条形按它重算。
- **读数浮层**：区域 / 省份行 → 营收 / 订单 / 环比同期 / 占全部（下钻后为占大区）+ 口径；逐日行 → 营收 / 订单 / 客单价。

#### B03 交叉表 Cross tab ——「列维度 · 规模阈值」两个维度交叉看一个指标。
- **数据**：行 = 6 个区域；列维度 渠道（3）/ 周（4）/ 日（22）；值 = 营收（万元）。
- **视觉**：行合计写在「小计」列（可排序），列合计写在末行；悬浮单元格时十字高亮所在行和列。
- **交互**：行 × 列超过 60 格（切到「日」= 132 格）时先出提示条，读者点「仍然展开」才展开；合计由显示值逐格相加。
- **读数浮层**：单元格（含小计格）→ 营收 / 占本行 / 占本列；行头 → 小计 / 占全部。

#### B05 分组小计 Grouped ——「组头折叠」组内比完，再组间比。
- **数据**：6 个区域 × 3 个渠道；默认展开华东、华南。
- **视觉**：每组一个 `<tbody>`；组头 `th scope=rowgroup` 写「华东 3 个渠道 · 小计」与小计数值；成员行的「组内占比」用数据条、「占全部」降为次要色。
- **交互**：组头的 ▸ 按钮（`aria-expanded` + `aria-controls`）展开 / 收起，→ ← 也可；「全部展开 / 全部收起」。

#### B06 可展开行 Expandable ——「独立展开按钮」看汇总，偶尔想看明细。
- **数据**：6 个区域，可展开到省份（省份值 = 大区 × 该省占比）。
- **交互**：行头里的 ▸ 是独立按钮（`aria-expanded` + `aria-controls`）；**行本身不下钻**（整行可点会和「下钻」语义打架）；→ 展开、← 收起；子行插在父行之后，父行位置不动；子行的占比写「占华东 40.8%」。

#### B07 对比列组 Column groups ——「排序 · 五态」本期、基期、差多少。
- **数据**：本期 9/1–9/22 与基期 8/1–8/22 的营收、订单，变化 = 差额（万元）+ 环比；合计 1,286.4 / 2,418 对 1,184.5 / 2,149，差额 +101.9，+8.6%。
- **视觉**：两行表头，三个列组各有淡底色（本期蓝、基期橄榄、变化紫，`color-mix` 7–9%）；合计行加深。
- **交互**：营收、订单、差额、环比都可三态排序；右上角切换五种状态（五层等高，实测差 <1px）。
- **无障碍**：列组 `th scope=colgroup colspan=2` + `<colgroup span=2>`；每个数据格 `headers="行头 组头 列头"`，读屏不必推断。

---

## 5. 无障碍与响应式规则

| 规则 | 实现 |
|---|---|
| 所有可选项为 `role=button` + `tabindex=0` + `aria-pressed`；滑动类为 `role=slider` + `aria-valuemin/max/now/valuetext` | 柱、格、段、扇段、气泡、数据点、节点；专注游标、目标线 |
| 每卡一个 `aria-live=polite` 区域播报状态变化；面板层接管后其它 live 区域设为 `off`，避免重复播报 | `.sr-only` |
| Esc 在任意卡片内取消选择 / 关闭面板 | 卡片级 keydown |
| 焦点环：页面 `#7CAC85`、SVG 内 `--c-focus`（深色白、浅色墨）、目标线把手 `--s-gold-text`（读数目标、探查区与指标 / 筛选组件的环样式见 2.1）；**焦点环只给键盘**：`<head>` 里的小脚本把最后一次输入方式写在 `<html data-input>`（`pointerdown` → `pointer`，除修饰键外的 `keydown` → `keyboard`），为 `pointer` 时隐藏全部焦点环（`:focus` 与 `:focus-visible` 两种都压，并加一条 `:focus:not(:focus-visible){outline:none}`，覆盖在普通 `:focus` 上画默认环的浏览器）与读数目标的发光，按任意键立即恢复；焦点移动逻辑不变（弹层回焦、排序后留在表头等照旧），只是不画环。文本输入框（F04 / F09 搜索框）保留 1px 边框变色 | `:focus-visible` + `data-input` |
| 浅色对比：正文 15.3:1、次级 8.8:1、标签 6.4:1、轴标签 5.1:1（面板内 5.3:1、停靠区 4.8:1）；彩色文字一律用 `-text` 变体（≥4.6:1）；色块上文字按亮度取墨或白（≥4.4:1） | 2.2 节 |
| 触屏：横向手势的 SVG 用 `touch-action: pan-y`，仅目标线用 `none`；F08 营收双滑块（`.fk-range`）也用 `none`；`-webkit-tap-highlight-color: transparent` | |
| ≤700px：读数浮层改为图下停靠区；SVG 文字最小 8.5px；树状图隐藏数值；顶栏 62px；单列 | `fitLabels` + 媒体查询 |
| 701–900px：单列、卡片最大 630px 居中 | |
| `prefers-reduced-motion` 与页面复选框同步；开启后动画直达终态、过渡 0.01ms，交互反馈保留 | `ChartKit.setReduced` |
| 指标组件：卡片为 `role=group` 且 `aria-label` 读作「客单价 5,320 元，环比（同期）下降 3.5%，向差」；箭头图标 `aria-hidden`；页签用 `role=tablist/tab` + `aria-controls`；指标条用 roving tabindex（整组一个 Tab 位）；记分卡的 `aria-sort` 写在当前排序列的 `th` 上（排序按钮在 `th` 内）；迷你图为 `role=slider` + `aria-valuetext` | 4.0 |
| 指标组件五态等高切换（实测差 <1px），隐藏层同步 `aria-hidden`，不会被读屏或指针命中 | K01 |
| 状态与涨跌不单独依赖颜色：状态胶囊 = 图标 + 文字 + 颜色；涨跌 = 箭头 + 数值 + 颜色 | 4.0 |
| 记分卡在窄屏横向滚动，指标名列 `position: sticky` 常驻 | P05 |
| 无外部资源：不加载字体、脚本、图片；单文件可离线打开 | |
| 筛选：单选下拉按 APG select-only combobox（`aria-activedescendant`，Enter / Space / Tab 确认）；多选为搜索 `combobox` + `listbox aria-multiselectable` + 三态「全选」`checkbox`；快捷项为 `radiogroup`（方向键即选中）；开关为 `role=switch`；月历为 APG 日期网格（单一可聚焦格、PageUp / PageDown 换月、`aria-selected`、读屏读全日期与状态） | 3.10 |
| 弹层关闭后焦点回到触发器；chip 移除后焦点落到下一个 chip；抽屉为 `role=dialog aria-modal`，焦点锁在抽屉内，关闭回焦「筛选」按钮 | F01–F08 |
| 页签：`tablist / tab / tabpanel` 互指；roving tabindex；自动 / 手动激活按「内容是否已在页面里」区分；纵向时 `aria-orientation=vertical` 并改用 ↑ ↓；徽标 `aria-hidden`，数量写进页签 `aria-label`；只给鼠标的 ✕ 设 `aria-hidden` + `tabindex=-1`，键盘另有等价按钮 | 3.11 |
| 表格：`<caption>`（可视或 `sr-only`）；行头 `th scope=row`、组头 `scope=rowgroup`、列组 `scope=colgroup` + 数据格 `headers`；`aria-sort` 只写在当前排序列；排序、翻页、勾选（「已选 N 行」，全部取消时「已取消全部选择」）、展开都向读数层的 live 区域播报；`TableKit` 表整表一个 Tab 位，↑ ↓ 逐行；B05 / B06 为手写表例外：每个 ▸ 各占一个 Tab 位，没有 ↑ ↓ 行导航，成员行 / 子行键盘到不了 | 3.12 |
| 横向滚动的表格外框只在真的溢出时才成为 `role=region` + `tabindex=0`；行头列 `position: sticky` 常驻 | 3.12 |
| 状态与涨跌不单独依赖颜色：日期面板的状态点另有图例与读屏全文；联动条件用虚线 chip + 来源文字（F07「来自图表联动」、F09「来自 区域营收 / 渠道构成」、B01「来自 明细表」） | F07 · F09 · F11 |
| ≤700px：纵向页签变横向滚动；抽屉占满卡宽且高度不超过视口；弹层宽度不超过 `100vw − 28px`；顶栏五个导航项单行横向滚动（右缘渐隐） | 4 · 筛选 / 页签 |
| 弹层与菜单下方放不下时翻到触发器上方，两边都放不下时滚进视口；滚动定位留出顶栏（`scroll-margin-top: 84px`） | 3.10 · T06 |

---

## 6. 与视频原始蒸馏的差异对照

| 控件 | 视频做法 | 基准版做法 | 性质 |
|---|---|---|---|
| Rings | 三环错峰 0.4s 依次画出 | 三环同步画出（1450ms） | 简化 |
| Heatmap | 17 周、按列填色、`MISSED` 标记、旁弹小卡 | 20 周、按列淡入、无 MISSED 标记、统一面板 + 键盘导航 | 功能替换 |
| Gauge | 三段 45/75，指针扫入→过冲→回落 | 三段 33/67，单段回弹缓动，可切换示例分数 | 参数调整 |
| Bars | 按住看值；切换先变高再冒新柱（两段式） | 点选看值；切换单段整体插值；28 天月视图；60 MIN 参考线 | 交互替换 |
| Focus | 从头滑动读数 | 默认停在 16:14；键盘滑块；底部小把手 | 增强 |
| Range | 折线 + 末值 8.2K | 平滑面积 + 范围平均值，更名"面积图" | 语义调整 |
| Goal | 拖把手 | 整条线可拖、键盘、2000–10000 步限幅 | 增强 |
| Stacked | 抽出约 8px | 上抬 14px + 引线；图例可点 | 增强 |
| Share | 点段外移、中心换字 | 相同，图例可点 | 一致 |
| Bubble | 被点者放大居中，其余被推开 | 聚焦模式：确定性槽位 + 弹簧碰撞，并声明大小不表示数量 | 语义澄清 |
| 全局 | 每控件一句提示词 | 统一读数浮层、筛选、动效开关、无障碍、5 个新图 | 扩展 |

视频原文提示词见附录 A，仍可作为 Vibe Coding 的输入句式。

---

## 7. 扩展约定（新增控件 / 修改现有控件）

1. **模块签名**：`ChartDemos.<id> = function (root, C) { …; return { reset }; }`。`root` 是 `.chart-host`，`C` 是 `ChartKit`。构造时写入 `root.innerHTML`（可含仅本卡使用的 `<style>`，类名加前缀）；图表与指标组件构造时调用一次 `reset()`，筛选 / 页签 / 表格组件（含 P05）只渲染当前状态、不调 `reset()`（它们的 `reset()` 会清掉地址状态，见第 10 条）。
2. **进场与重播**：所有补间用 `C.animate(key, …)`，key 全局唯一（实例化多份时用序号）；`reset()` 必须能从任意状态恢复初始态。
3. **指标适配器**：必须提供 `root.inspector`（缺失会抛错）；自带的提示元素放进 `legacy` 让面板层隐藏；连续型图表提供 `probe`。
4. **选中约定**：`aria-pressed` 表达选中；其余项降噪；Esc 取消；切换范围清除选择；变化写入本卡播报区（读数层的 live 区域，见 3.3「每卡一条播报通道」），不要自建 `aria-live` 区域——mount 会把它静音。
5. **登记**：在页尾 `demos` 数组追加 `[id, 编号, 中文名, 英文名, 分组, 手势标签, 一句话标题, 说明, 附加类名?]`；分组取 `progress | time | share | structure | kpi`；指标组件附加类名写 `kpi-spec`（面板再加 `kpi-wide` 跨两列），并在 `DIVIDERS` 里登记分组分隔标题；同步更新筛选按钮计数、引言统计与 `collection-count`。
6. **坐标与尺寸**：viewBox 宽 480（或 460），高 220–260；`.chart-head` 占 56px；文字使用 viewBox 单位，窄屏由 `fitLabels` 兜底。
7. **指标组件另加**：数值一律经 `KpiKit.fmt` 输出，禁止在模板里手写「万 / 亿 / %」；派生值现算不硬写；好坏色只由 `polarity` 决定；新指标要同时给 `MET` 里的 `kind / polarity / change / note`。
8. **色彩**：图表代码里不写十六进制，只写 `var(--token)`；主色从 2.2.2 取一个，分类用鼠尾草绿 / 橄榄 / 中性 / 灰；动态改色走 `C.paint`，彩色文字用 `C.tone(color, "text")`，色块上的文字用 `C.tone(color, "on")`；新增颜色先在 token 表补齐深浅两个值并跑一遍校验脚本；状态色 `--st-*` 不复用为系列色。
9. **验收**：Chromium 无头跑 深 / 浅 × 1320 / 800 / 390 → 无横向溢出、控制台 0 报错；键盘可达；减少动态效果下功能完整；切换主题不丢状态；指标组件另跑交互断言（切换后的数值、排序后的顺序、五态等高、重播复位）；本文相应小节已更新。
10. **筛选 / 页签 / 表格组件另加**：
    - 登记时分组取 `filter | tabs | table`，附加类名写 `ftt-spec`（需要两列宽再加 `ftt-wide`），在 `DIVIDERS` 的该组首项（F01 / T01 / B01）登记分隔标题，并更新筛选按钮计数、引言统计与 `collection-count`。
    - 写在筛选模块内：`Hash`、`AsOf`、`touched`、`lookups` 与 `FK / TK / TBK` 只在模块内可见，页面全局只有 `FilterData / FilterKit / TabKit / TableKit`。
    - **不产生数据**：读数来自 `FilterData.q / qBase / geo`（都按截至日截断）；逐日读数用模块内的 `dayAt / spanAt`，不用不看截至日的 `FilterData.span / dayVal`；新事实加进 `FACT` 时一并补 `check()` 勾稽；可相加的列交给 `TableKit` 的 `sum:true`（最大余数分摊），不在模板里手算合计。
    - 状态写地址：键名 = 组件编号小写（`f12`、`t09`…），**默认状态不写**；`reset()` 里用 `Hash.set(key, null, {push:false})` 清掉；构造时读到地址状态就调用 `touched(root)`，避免进场重播把它冲掉。
    - 跟随截至日：订阅 `AsOf.on(render)`；日期上限用函数形式（`max: () => AsOf.key()`），不要把 9/22 写死。
    - 弹层一律用 `FK.pop`，页签一律用 `TK.create`；表格优先用 `TBK.create`，B05 / B06 为手写例外，待迁移；自己写的菜单要补同样的翻转与回焦规则（参照 T06）。
    - 动态生成的读数目标（行、条形、格子）加 `data-metric-target`，并在 `root.inspector.lookup(el)` 里返回 `{el, read}`；多个来源用 `lookups(a, b, …)` 串起来。
    - 面板高度：同一组页签面板共用最小高度，按最高的那个设；内容明显偏少的面板先补内容（走势线、结构条、要点），不要靠留白撑高。
11. **验收另加**：`npm test`（即 `node tests/run.mjs`）一键跑全部检查并逐项给出 ✓ / ✗：产物与源码一致；`fint`（98 项：键盘、地址复原、截至日传播、行点击四种模式、三态排序、五态等高、读数动态查找）与 `kint`（指标组件 50 项）两套主题；三档视口无溢出、控制台 0 报错；键盘焦点环；「console 没有 assert」模拟；收尾修复回归（26 项）；读数浮层移开与触屏检查。`npm run test:full` 另跑读数浮层扫动（面板始终跟到指针）与 6 倍降速开销。弹层 / 菜单 / 抽屉在 1320 × 844 与 390 × 844 下完整可见。
12. **运行环境**：页面要能在宿主受限的环境里跑（Artifact 查看器、各类预览框）。
    - 不直接调用 `console.assert` 等非标准 console 方法：宿主的 console 可能没有它们，一次 TypeError 会让整页脚本停止。数据自检用模块内的 `check(ok, msg)`，不通过时写 `console.error`、不抛错。
    - 指标模块与筛选模块各自整体包在 `try / catch` 里：模块初始化失败只停它自己，前面的图表照常工作。
    - 页面按卡隔离构建：某张卡构造或挂读数层失败时，卡内写「这个组件没能加载（原因），其它组件不受影响」，其余卡照常；重播全部不受影响。
    - `localStorage`、`history.pushState` 一律包 `try / catch`（已有）。
    - 验收时另跑一遍「console 没有 assert」的模拟，确认 51 张卡全部建出。
    - 焦点环只给键盘：不要写只靠 `:focus` 的样式；压焦点环时 `:focus` 与 `:focus-visible` 分开写两条（不支持 `:focus-visible` 的浏览器会整条丢弃含它的选择器列表）；用 JS 画焦点标记时同时判断 `document.documentElement.dataset.input !== 'pointer'`（参照组合图）。代码里移动焦点照常写，环由 `data-input` 统一控制 —— 各浏览器对「脚本移动焦点后要不要显示环」的判断不一致（Chrome 不显示，Safari 会显示）。
    - 验收时用鼠标与触屏各点一遍全部控件：除文本输入框外，不应出现焦点环；随后按任意方向键，焦点环应立即出现。
13. **读数适配器与悬停的开销**（读数浮层每次指针移动都会调用适配器）：
    - `probe.hover(i)` 只移动标记（导引线、空心点、强调），不重新量尺寸、不重设 `viewBox`、不重画整张图；整图重画只在尺寸变化、进场、切换数据时做（参照 P02 / P03 的 `mark()` 与 `KpiKit.spark`）。
    - `read()` 里格式化数字用缓存的 `Intl.NumberFormat`（`KpiKit.nf` 已按精度缓存），不要每次调用 `toLocaleString(…, options)` —— 它每次都新建格式器。
    - 图表自己的 `pointermove` 落在同一个点上时什么都不写（参照 `KpiKit.spark` 的 `set()`）。
    - 验收：以每帧 6px 横竖扫过每张卡，面板可见时始终贴在指针旁（没跟上的采样为 0）；点击后移出卡片、在卡内空白持续移动，面板分别在 450ms / 600ms 内关闭；宽屏触屏点图表外即关闭，窄屏停靠区保留最后一次读数；CPU 降速 6 倍时每次移动的处理耗时 p95 ≤ 16ms。
14. **源码与构建**（仓库 `Dashboard-Design`）：
    - `chart-studies.html` 是构建产物，**不要直接改**。源码分三块：`src/page.html`（页面模板：外壳、15 个图表、`ChartKit`、`ChartInspector`、启动代码，含四对注入标记）、`src/kpi/`（`kpi.css` + `kpi1–5.js`，指标模块）、`src/ftt/`（`ftt.css` + `ftt0–5.js`，筛选 / 页签 / 表格模块）。
    - `npm run build`（`node tools/build.mjs`）拼接模块、逐段做语法检查、注入模板，写出 `chart-studies.html`；`--artifact` 另生成 `dist/chart-widgets-demo.html`（发布到 Artifact 的正文，本文件作为同目录附件）。
    - 改完源码：构建 → `npm test` → 更新本文相应小节与第 8 节 → 连同产物一起提交。

---

## 8. 修复记录（v01 基准之上）

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 8.1 | `ChartKit.animate` | 首帧 rAF 时间戳可能早于 `performance.now()`，`t<0` 使 ease-out 立方函数输出负值 → 目标线、组合图柱高为负，控制台报 `<rect> attribute height: A negative value`（每档视口 19 条） | 帧进度夹紧 `Math.min(1, Math.max(0, …))` |
| 8.2 | 气泡图 `draw()` | 数值文字从 r≈25 起渐显，Swim（r≈26.6）在比例模式残留 17% 透明度的「40 MIN」 | 渐显区间改为 `(r−27)/5`，半径 ≥32 才完整显示 |
| 8.3 | `ChartInspector.mount` | 面板层给 probe 型 SVG 覆盖了描述性 `aria-label`（面积图 / 折线图 / 组合图变成通用文案） | 保留原描述并追加「；左右方向键选择采样点」 |

| 8.4 | 全局色彩 | 143 个字面色值散落在 CSS 与各图表模块，无法换主题 | 图表层全部改为 99 个语义 token；页面层（纸色区域）保留字面值。深色值取基准原值，近似灰合并（最大 ΔE 4.2）；逐卡像素比对平均差 ≤0.05/255 |
| 8.5 | 圆环 EXERCISE | 原用"白色"兼作系列色，浅色下需要变为墨色 | 拆出 `--s-ink` 系列（深色 `#f1f0e9` 与原值一致） |

| 8.6 | 状态色 | 指标卡的状态胶囊把 `-text` 写在自身淡色底上，浅色三色实测 3.7–4.3:1，不足 4.5 | 新增 `--st-*-soft` 胶囊底 token，并把浅色 `-text` 加深到在胶囊底上仍 ≥4.6:1（`#028345→#03793f`、`#d23834→#c62b2a`、`#9c6803→#936100`）；token 总数 99 → 102 |
| 8.7 | 指标卡布局 | 固定高度的 `.chart-host` 让数值卡下方留大片空白 | 指标组件改为内容定高，同一行两张卡用 flex 拉平 |
| 8.8 | 指标条键盘 | 方向键移动焦点后，读数层的 root keydown 仍按旧目标重读，浮层显示上一个指标 | `KpiKit.roving` 在移动焦点后 `stopPropagation()` |

复测：深 / 浅 × 三档视口控制台 0 报错，负高度写入 0 次，深色视觉无回归，浅色全部 29 张卡与交互态已目检；指标组件 50 项交互断言两套主题全绿。

**筛选 / 页签 / 表格一轮（2026-09-23）**

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 8.9 | B03 交叉表 | 表格首次渲染时 `onRender` 回调里引用了还没赋值的实例（`T`），页面报 `Cannot read properties of null` | 回调里改用 `qk(root,'tb')` 取表格，不依赖实例变量 |
| 8.10 | 筛选 / 页签容器 | 组件自身的 `display` 规则压过了 `[hidden]`，空态、分页器等本应隐藏的元素露出 | `.ftt-host [hidden]{display:none!important}` |
| 8.11 | F01 对比口径 | 「本月」是 23 天（有数据 22 天），对比期取了上月 23 天，环比被低估 | 本期与对比期都只在有数据的日子上比（按天平移后求和） |
| 8.12 | F04 / B03 类名 | F04 结果区的 `data-k="count"` 与多选弹层底部计数撞名；B03 小计列类名 `sub` 撞上 `.tb .sub{display:block}` | 改名 `selcount`、`subtot` |
| 8.13 | `TabKit` 初始化 | 构造时调用 `onSelect` 有暂时性死区风险；构造时 `scrollIntoView` 会把页面滚走 | 构造不回调；只有键盘、点击、菜单触发时才把页签滚进可视区 |
| 8.14 | T08 重建 | 新建 / 关闭视图时重建页签，旧的键盘监听叠加，一次按键切两次 | 重建时 `cloneNode(false)` 换掉 tablist，地址监听单独挂一次 |
| 8.15 | 截至日 | 月历的可选上限在截至日改变后不更新；查询没有夹到截至日；带 `asof` 的分享链接首次进视口被进场重播重置 | 上限改为函数；`q()` 终点夹到截至日；截至日不是最新时卡片记为已操作 |
| 8.16 | T05 处理按钮 | 点「标记已处理」后 `render()` 替换了按钮，之后再读 `b.closest()` 得到 `null`，报错并丢焦点 | 先读出所在分组与按钮文字，再重绘 |
| 8.17 | 弹层 Esc | 弹层刚打开、焦点还在触发器上时按 Esc 无效 | 触发器上也监听 Esc |
| 8.18 | 弹层位置 | 定位发生在内容生成之前，按空弹层的高度判断「放得下」，靠近视口底部时弹层开到屏幕外；T06 菜单没有翻转，打开时聚焦把页面滚走约 470px | `onOpen` 之后再定位一次，两边都放不下就滚进视口；T06 菜单同样翻转，聚焦用 `preventScroll` |
| 8.19 | T01 趋势 | 固定 viewBox + `max-height` 让图表在宽卡里居中缩小，两侧各空约 200px；重绘用 `replaceChildren` 把读数层的导引线也删了 | 按真实像素宽设 viewBox 并在尺寸变化时重绘；图形画进单独的 `<g>`，导引线保留 |
| 8.20 | T07 窄屏 | 纵向指示条的 `width:2px!important` 在变成横向后仍生效，选中下划线只剩一个点；滚动容器裁掉指示条下半 | 宽度规则只留给纵向；横向时 `bottom:0` |
| 8.21 | T06 / T08 焦点环 | 页签在横向滚动容器里，外扩的焦点环上下被裁，只剩两条竖线 | 这两处焦点环画在页签内侧（`outline-offset:-3px`） |
| 8.22 | 示例数据 | 六个大区与三个渠道的客单价几乎相同（5,315–5,361 元），渠道与客群的环比全部是 +8.6%，表格里像是算错了 | 新增大区 × 渠道订单与渠道 × 客群订单事实（客单价 4,042–6,450 元）；基期结构按渠道 / 客群增速反推；所有合计与 KPI 一致 |
| 8.23 | 表格列宽 | 数据条列吸走多余宽度，B05 的「组内占比」列中间空出一大段 | 数据条列 `width:1%` 收缩到内容 |
| 8.24 | 顶栏（390px） | 导航增加到五项后，品牌与每个导航项都折成两行 | ≤700px 导航单行横向滚动，右缘渐隐 |
| 8.25 | F08 抽屉（390px） | 抽屉与卡片等高、比视口高，「应用」按钮在屏幕外 | 抽屉高度不超过视口，打开时整只滚进视口 |
| 8.26 | F09 提示行 | 模式提示用了错误色和【】括号，像报错 | 改为灰色说明文字「联动开 · 单击条形即筛选其余两图…」 |
| 8.27 | F05 标签 | 「鼓楼（南京）」在 44px 标签列里折行 | 条形标签列宽改为变量 `--lab`（F05 用 84px），超长省略 |
| 8.28 | 页签面板留白 | 面板统一 300px 最小高度，总览、结构、纵向页签各面板下方大片空白；T05 列表 2–10 条时卡片高度跳动 | 补内容（走势线、要点、营收 / 订单结构条、逐日小柱、及时率走势），最小高度按组取最高面板；T05 列表区固定高度内部滚动 |
| 8.29 | 数据自检 / 页面初始化 | Artifact 查看器的 console 没有 `assert`：指标、筛选两个模块加载时的 13 处 `console.assert` 抛出 `TypeError: console.assert is not a function`，整页脚本停止，51 张卡一张都没建出来 | 改为模块内 `check()`（不通过写 `console.error`，不抛错）；两个模块各自 `try / catch`；页面按卡隔离构建。已模拟三种失败验证：单张卡抛错（50 张照常）、指标模块失败（15 张图表照常）、筛选模块失败（图表与 13 张指标组件照常），均无未捕获错误 |
| 8.30 | 点击后的焦点框 | 用鼠标 / 触屏逐一点击与拖动 51 张卡的全部控件（约 490 次）：Chrome 里专注分段图点击或拖动后出现 1px 虚线框（按下时脚本 `focus()`，被判为 `:focus-visible`）；另有 16 个组件在点击后由代码移动焦点（共 40 种点击路径：F01 / F02 / F03 / F04 / F05 / F09 弹层打开与回焦、F07 / F08 移除条件与抽屉开关、T06 更多菜单、T08 新建 / 关闭视图、B01 / B03 / P05 / T01 明细 / F08 结果表 排序、B03 周列、B05 / B06 展开），Chrome 不画环，但 Safari 等浏览器会画出 2px 粗框 | 页面记录输入方式（`data-input`），指针操作后隐藏所有焦点环，按键恢复；组合图的 JS 焦点标记同样判断输入方式。复测：鼠标与触屏两轮点击只剩 F04 / F09 搜索框的 1px 边框；键盘 12 项检查（Tab、方向键、点击后再按键、目标线把手、专注分段图）全部出现焦点环 |
| 8.31 | 点击后的焦点框（第二轮） | 在用户的浏览器里 P02 主次面板、P03 指标页签驱动图表、K05 进度环卡点击后仍出现 2px 橙色圆角框（浏览器默认焦点环，颜色取系统强调色）：这类浏览器在普通 `:focus` 上画默认环，而上一轮只压了 `:focus-visible`。在 Chrome 里注入同样的「`:focus` 默认环」复现：已发布版本 51 张卡点击后都会出框（至少「重播」按钮），P02 / P03 / K05 与截图一致 | 指针模式下同时压 `:focus`，并加 `:focus:not(:focus-visible){outline:none}`（两条分开写）；输入方式另监听 `mousedown` / `touchstart`。复测：同样的模拟下逐一点击 51 张卡全部控件（486 次），除 F04 / F09 搜索框的 1px 边框外为 0；键盘 12 项检查全部仍有焦点环 |
| 8.32 | 读数浮层 · 跟随与关闭 | 反馈「移动卡顿、移走后面板不消失」，查到四处：① 指针落在两个图形之间的空隙或卡内空白时只走「准备关闭」分支、不更新位置，面板停在原地，进入下一个图形再跳过去 —— 以每帧 6px 扫过 8 张卡，已发布版本 24% 的采样面板没跟上指针，最远落后 205px；② 这个分支每次移动都把 120ms 关闭计时器重新开始，只要鼠标还在卡内移动就永不关闭；③ 鼠标点击处于选中态的元素会固定面板，移出卡片后面板弹回原处不走；④ 宽屏触屏点一下固定后，点页面别处也不消失 | 空隙里面板继续跟随指针，宽限期只计一次；鼠标点击不再固定（触屏 / 窄屏保留），宽屏触屏点图表外即取消；已有面板显示中或刚关不到 300ms 时，下一个直接出现。复测：47 张可悬停的卡横竖扫动共 8,608 次采样，「没跟上」0 次；点击后移出、卡内空白持续移动两项 47 / 47 按时关闭；宽屏触屏 8 / 8 点外即关，窄屏停靠区 8 / 8 保留读数 |
| 8.33 | 读数浮层 · 每次移动的开销 | 面板用 `left / top` 定位，每帧都要重新布局并重绘它下面的图表；每次移动重复写入没变的属性、新建媒体查询对象、用 `toLocaleString(…, options)` 格式化（每次新建格式器）；P02 / P03 与指标卡迷你图悬停换点时整图重画（重新量尺寸、重设 `viewBox`），迷你图在同一点上移动也重画。CPU 降速 6 倍时 P02 每次移动 p95 20.6ms、P03 17.0ms，扫动期间 12% 的帧超过 34ms | 面板改用 `translate3d` + `will-change` 移动；只写变化了的属性；缓存媒体查询与 `Intl.NumberFormat`；悬停只移动标记（`mark()`），同一点不重复写。8 张卡各扫 41 帧：布局次数 465 → 113，布局耗时 −62%；6 倍降速下 p95：P02 20.6 → 10.8ms、P03 17.0 → 12.2ms、折线图 9.3 → 5.7ms，超过 34ms 的帧 12% → 1.3%；K03 / K09 迷你图拖动时超过 34ms 的帧 38 → 0 |

复测：深 / 浅 × 1320 / 800 / 390 共 51 张卡无横向溢出、控制台 0 报错；筛选 / 页签 / 表格 98 项交互断言与指标组件 50 项交互断言两套主题全绿；八个弹层 / 菜单 / 抽屉在 1320 × 844 与 390 × 844 下完整可见；每个页签的每个面板在 1320 与 390 下逐张目检。读数浮层两项（8.32 / 8.33）修复后，以上断言、键盘 12 项与「console 没有 assert」模拟全部复跑通过。

**第一阶段收尾对齐（2026-09-23）**：逐节核对本文与页面（133 处出入，本文已按页面改正或补全），其中下列问题改在页面上。

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 8.34 | 地址 · 后退 | 后退 / 前进到「全部默认」那一步（地址无哈希）时状态不复原：只有 `#!` 开头的哈希才通知订阅者 | Hash 写入的历史条目与页面初始条目带 `history.state` 标记，带标记即通知；普通锚点仍不影响状态 |
| 8.35 | F05 / F06 / F08 多选顺序 | 按点击顺序编码与比较：F05 取消再勾回默认项会写出一个等于默认值的地址，F06 同一组合有两种写法，F08 抽屉显示「改动 1 项未应用」 | 按固定顺序（区县叶子 / 渠道·客群 / 大区）编码与比较 |
| 8.36 | 截至日 · 分享链接 | 用带 `asof` 的链接打开时 K04 / K05 仍按 9/22：它们只听 `chart-asof`，而截至日从地址初始化时不派发 | 页面搭建完成后补派发一次 |
| 8.37 | F01 / F02 月历上限 | 8.15 只改了 F11：F01 / F02 的上限仍是构造时的常量，截至日改早后还能选之后的日子，「回到最新」后选不到 9/15–9/22 | 改为 `max: () => AsOf.key()` |
| 8.38 | T05 焦点 | 「标记已处理」后焦点跳回同组第一条 | 落到同一位置的下一条；列表变短时落到最后一条，空了回到页签 |
| 8.39 | 勾选 / 换月播报 | 表格勾选行不播报；月历标题自带的 live 区域被读数层静音，点 ‹ › 换月无声 | 都写本卡播报区：「已选 N 行」/「已取消全部选择」、「2026 年 8 月」 |
| 8.40 | K04 / K06 胶囊 | 每次换档重建胶囊元素，`.kpi-chip` 的颜色过渡从不生效 | 用 `setChip` 原位换档 |
| 8.41 | 06 面积图 | 切换与首次进视口的重播重写 `aria-label`，读数层追加的「左右方向键选择采样点」丢失 | 读数层挂载后重写时保留这句提示 |
| 8.42 | 文案 | `<meta description>` 仍写「十五种可交互图表」；F11 说明与播报写「全页重算」，实际只有筛选 / 页签 / 表格与 K04、K05 跟随截至日 | description 改为 51 种组件；F11 文案写明实际范围 |
| 8.43 | F07 / 月历 / B05 · B06 | F07「添加条件」禁用时原因只在 `aria-label`；月历周末表头在浅色主题只有 3.2:1；B05 / B06 手写表横向溢出时外框没有 `role=region` | 加 `title`；表头改为平日 `--c-text-3`、周末 `--c-text-4`；按 TableKit 的规则只在溢出时设为有名称的 region |

复测：新增 `tests/regress.mjs` 26 项断言覆盖以上修复（在修复前的页面上 21 项失败），两套主题的交互断言、三档视口、键盘焦点环、「console 没有 assert」模拟与读数浮层检查全部复跑通过。

---

## 9. 待办与建议

**已决（2026-09-23，24 项拍板）**

模板级 3 项：

- **热力格 MISSED 标记**：保持现状，不加标记（见 4 · 02 备注）。
- **组合图双轴**：保留，写清适用条件（见 4 · 12 备注）。
- **深色辅色色差**：保持现值（2026-09-23 看过逐卡调整前后对比后决定）。测算结论留档：深色主题里金黄、鼠尾草绿、浅紫、米白处在同一高亮度带，要让占比条 / 环形 / 气泡里的相邻色对都达到正常视觉 ΔE ≥ 15，鼠尾草绿至少要移动 ΔE 20（`#b8c7ab → #6e8872` 一类的中灰绿），并连带把石板灰调深；这超出了「微调」，故不改。现有做法仍是直接标注 + 分段缝隙补偿（占比条、环形图都有图例与读数，气泡有名称）。

筛选 / 页签 / 表格 21 项（已全部落地，见第 4 节三组的「共用规则」）：

| 组 | 决策 → 结论 |
|---|---|
| 筛选（9） | 首批 → 十一项一次做完 · 生效 → 即时 + 抽屉批量 · 时间上下文 → 截至日 + 卡内口径 · 点击图表 → 单击直接联动 · 联动开关（拍板时追加）→ 看板级，默认开 · 级联 → 整条路径匹配 · 日期面板 → 状态点按星期加权 · 状态写地址 → 写进 hash · 外观 → 输入框方角、选项药丸 |
| 页签（6） | 首批 → 六种一次做完 · 激活 → 分情况（内容在页内自动，要取数手动）· 样式 → 页面级下划线、卡内药丸 · 分段控件 → 保持按钮组 · 层级 → 最多两层 · 地址 → 选中页签写进 hash |
| 表格（7） | 首批 → 五种一次做完 · 排序 → 三态 · 斑马纹 → 分隔线为主 · 条件格式 → 数据条 + 状态胶囊 · 行点击 → 都兼容，按表的用途配置，不写死 · 列组表头 → 真跨列（colspan）· 长表 → ≤ 50 行整表，超出分页 |

方案原文：[《筛选器组件方案》](https://claude.ai/artifact/LoifuCkEE1jVvzxHbE8Jnc) · [《Tab 组件方案》](https://claude.ai/artifact/EZPkHwQ8a7MqXJxcx5qC7o) · [《表格组件方案》](https://claude.ai/artifact/WoQdwRtxaxVRVLiJc4JboE)（均已写回「已定」与落地说明）。仓库内另有四份方案的离线副本：`docs/proposals/`。

**进行中**

- 无。

**建议（未实施）**

- **指标组件**：方案见[《指标卡与指标面板方案》](https://claude.ai/artifact/C6rFoUBaTKgn6Z1Qx6FyYZ)，首批 14 项已并入模板（第 4 节「指标与概览」）；K07 构成卡、K10 翻牌器、P06 指标树按决策暂缓。
- **截至日范围**：`AsOf` 目前只在 9/1–9/22 之间移动，指标组件里只有 K04 / K05 跟随。接真实数据时建议把截至日做成任意日期，并让其余指标组件也订阅 `chart-asof`（`scAt` 已能按天重算）。
- **跨卡联动**：F09 的联动只在一张卡的三图之间；看板级跨卡联动可以直接复用同一个条件模型（`q()` 的 `f`）和虚线 chip。
- **地址键名**：模板独占页面时 `f01 / t01 / asof` 足够；嵌进宿主页面时建议加命名空间前缀（如 `cs.f01`）。表格的排序、翻页目前不写地址，需要分享某个排序视图时可按同一规则加 `b01=rev.desc`。
- **写操作**：T05「标记已处理」、T08「新建视图」只存在内存里；接后端时改为乐观更新 + 失败回滚，并保留现在的焦点落点。
- **数据接入**：`DATA` 与 `FACT` 都是固定事实；接真实数据时保持「事实 + 现算派生」的结构，并保留两个模块顶部的 `check()` 勾稽（指标 5 条、筛选 8 条）。各图表模块的数据仍是函数内常量，建议改为 `ChartDemos.<id>(root, C, data)` 并保持 `reset()` 语义。
- **进场重复**：构造时与首次进入视口各播一次进场（可见卡片会在首帧后重启一次），肉眼不可察，可在 IO 回调中跳过刚构造的卡片以省一次动画。
- **B05 / B06 迁到 `TableKit`**：两张表仍是手写的，没有 ↑ ↓ 行导航，每个 ▸ 各占一个 Tab 位，成员行 / 子行键盘到不了（读数只能靠悬浮）；迁移时保留组头小计、独立展开按钮与 → ← 展开收起。
- **原有图表的播报**：15 个原有图表自带的切换 / 选中播报区被读数层静音（3.3「每卡一条播报通道」），示例分数、范围切换、目标线松手等操作都不主动播报；指标组件（K04 周期、P01 口径、P04 周期等）的切换也不播报。建议由 `ChartKit` 提供共用的 `say(root, msg)`，这些切换各写一句结果摘要到本卡播报区。
- **矩阵季度切换**：Q1 / Q2 切换保留选中（便于追踪迁移），与页面「交互说明」里的「切换时间范围后清除旧选择」不一致；二选一：代码清除选中，或把说明改为「季度切换保留选中，用于观察迁移」。气泡按业务固定配色，若要表达当前象限，可改为按季度取象限色并插值。
- **组合图图例标轴归属**：图例目前只写「营收 / 成本 / 毛利率」，轴归属只靠左轴顶「万元」与右轴 % 刻度；建议写成「营收 · 左轴 / 成本 · 左轴 / 毛利率 · 右轴」，补齐 4 · 12 备注第 4 条。
- **截至日胶囊文案**：改动后写「全页截至 9/14」，与 F11 一样说大了范围（15 个图表与多数指标组件不跟随）；建议改为「截至 9/14」一类说法。
- **`TableKit` 细节**：延迟提示条与失败文案写死在组件里，建议开放成选项（`staleText`、`errorText`）；斑马纹规则按当前页可见行数判断，分页表永远不会触发，建议改按总行数或删掉。
- **清理**：`KpiKit.canvas()` 没有组件使用（P02 / P03 用 `ResizeObserver`），可删或改用它；三个已隐藏的旧提示框（柱状图、分段占比条、热力格）及只被它们引用的 `--s-mint` / `--s-mint-text`、从未使用的 `.chart-tip` / `.empty-filter`、已被覆盖的 `.chart-host` / `.chart-svg` 高度声明（见 1.2）都可清掉。≤700 时 `.kpi-host` 继承了 350 的最小高度（`.ftt-host` 已解除），确认是否有意。页面「阅读层级」清单写「类别与数值 12–14 px」，实际图例数值为 15px，二选一对齐。

---

## 附录 A · 视频原始蒸馏（出处保留）

**来源**：抖音 @叨叨 AI《10个图表交互控件提升App和小程序的质感》（3′39″）。视频以虚构 App「Tally / Insights」演示 10 个控件，每个配一句可直接喂给 AI 的提示词。

### A.1 通用视觉语言（视频）
近黑卡片、一控件一主色 + 共用中性辅色（灰绿 / 卡其 / 米白 / 灰阶）、全大写宽字距小标题 + 白色粗体主数值、圆角与圆线帽、分段留细缝、面积用主色到透明的渐变、触点为半透明圆环涟漪。

### A.2 通用交互模式（视频）
从零画起 · 数字同步 · 错峰 · 过渡不重绘 · 焦点 + 降噪 · 就地读数 · 直接操纵 · 微物理（过冲回弹 / 推开 / 抽出）· 状态即结论。

### A.3 十句提示词（原文）

| # | 控件 | 提示词 |
|---|---|---|
| 01 | Rings | 做一组同心活动圆环，进入时从零画到当前值，中心数字同步增长，超出部分叠加深色。 |
| 02 | Heatmap | 做一个打卡热力格，按次数分五档深浅，点击某一天弹出当天详情。 |
| 03 | Gauge | 做一个半圆仪表盘，刻度分三段颜色，指针进入时扫到当前值，带轻微回弹。 |
| 04 | Bars | 做一个柱状图，切换周和月时柱高平滑过渡，按住某根时其余变暗并显示数值。 |
| 05 | Focus | 做一个专注分段图，按阶段区分高度和颜色，横向滑动时顶部显示当前时刻和阶段。 |
| 06 | Range | 做一个折线图，顶部切换一周、一月、一年，线条平滑变形到新数据，面积渐变同步过渡。 |
| 07 | Goal | 做一个带可拖动目标线的柱状图，拖动时过线的柱子高亮，顶部实时显示达标天数。 |
| 08 | Stacked | 做一条分段占比条，段间留细缝，点击某段时抽出并显示名称和百分比。 |
| 09 | Share | 做一个环形占比图，各段依次顺时针画出，点击某段外移高亮，中心显示该段名称和百分比。 |
| 10 | Bubble | 做一个气泡图，圆的大小表示数量，点击某个气泡放大居中，其余气泡被推开。 |

句式：「做一个 X，[静态规则]，[进场动效]，[手势 → 反馈]」。新增 5 个控件可沿用此句式补写提示词，例如：「做一个瀑布图，期初期末用总量色，正负贡献用两种颜色并用虚线连接柱顶，切换实际 / 预算时各段连续变化，点击某段显示变化前后累计。」

### A.4 视频色值（抽帧估算，供对照）
琥珀 `#EAAE42`（超出段 `#7E5A1E`）· 薄荷 `#66C6A2` · 珊瑚红 `#D24E4E` · 天蓝 `#7EAED2` · 黄绿 `#BAD25A` · 淡紫 `#A494AC` · 橙 `#DE724E` · 黄 `#EACC44` · 粉 `#DE7EA2` · 蓝紫 `#9494E4`；中性：灰绿 `#ACBCAC` · 卡其 `#A49C74` · 米白 `#E4E4DC` · 深灰 `#3C3C44 / #54545C`；卡片 `#0C0C0E`。
